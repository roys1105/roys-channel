# Roy's Channel（メインサイト）開発ログ

このファイルは、作業するたびに上に追記していきます。
触ったファイルの一覧は `../../DEVLOG_auto.tsv`（`dev/DEVLOG_auto.tsv`）に自動記録されています。

---

## 2026-09-24（木）その2 — 「相棒の黒猫クロ」カードにLINEスタンプへの小さなリンクを追加（公開ずみ）

### やったこと
- `index.html`：`#kuro` の4枚のスタンプ風画像の下に、緑の小さな丸いリンク「LINE クロのスタンプ発売中 →」（`.kuro-line`）を追加。販売URLへ別タブ。説明文は増やしていない。

### わかったこと・つまずいたこと
- 特になし。

### 次にやること
- ロイさんの合図で push。

### 動作確認
- ローカル：リンク先URL・1行表示・横スクロールなしを数値で確認。ライブは未確認。

## 2026-09-24（木）— LINEスタンプ「ゆるっと黒猫クロの日常」の紹介セクションを追加（公開ずみ 2026-09-24）

### やったこと
- `index.html`：お知らせ欄の直下に `#line-stamp` セクション（画像・説明・緑の購入ボタン）を追加。ヘッダーのメニューにも「スタンプ」を追加。販売URLは https://line.me/S/sticker/36834372（別タブで開く）。
- `image/line-stamp-kuro.png`：LINEストアのメイン画像（240×240）を保存。

### わかったこと・つまずいたこと
- このファイルの改行は LF（senior-site と違い CRLF ではない）。メニュー名は「LINEスタンプ」だと幅の狭い画面で2行に割れたので「スタンプ」にした。

- 【追記】メニューが2行に割れた：項目を1つ足したせいで、ハンバーガーに切り替わる900px直前で横に収まらなかった。`nav.pc a` に `white-space:nowrap`、gap を24→18px、ハンバーガーへの切り替えを1100pxに引き上げて解消（1101px・1120pxで1行・はみ出しなしを数値確認）。

- 【追記2】ヒーローのボタン2つを同じ形・同じ色（btn-primary）に統一。ボタンの下に「講座・教材をさがす」（表紙つきカード3枚：はじめてのスマホ教室→#senior／独りで稼ぐ教科書→#kouza／Claude入門→#kouza-claude）を追加（CSSは .hero-learn / .hl-*）。スマホ幅は縦並び。

- 【追記3】「講座・教材をさがす」を hero-copy の外（`.hero-learn-wrap`）へ出して横幅いっぱいにし、カードは横並び（表紙＋題名＋矢印）に。これでキャラクターの足元がボタンの行の下端にそろう。

- 【追記4】ヒーローの余白を詰めた（.hero-copy の下 34→8px、.hero-learn-wrap の上 40→26px、キャラの下マイナス余白 -28→-8px）。ヒーローのボタンだけ色をやわらげた（#4A8AA3、`.hero-cta .btn-primary`。サイト他所の btn-primary は従来の濃い紺のまま）。吹き出しをキャラの顔の左に置き、しっぽを右（顔の側）へ向けた。

- 【追記5】吹き出しを楕円のバルーン形（border-radius:50%）にし、しっぽを右下の斜め向き（clip-path の三角2枚重ね）に変更。位置は顔の左上寄り。

- 【追記6】吹き出しを小さく（padding 20/34→15/26px）、しっぽを長い斜めの三角に（顔の左＝サングラスのほうへ）。

- 【追記7】吹き出しをインラインSVG（傾いた楕円＋カーブしたしっぽが1本の線）に作り替え。CSSの ::before/::after のしっぽは廃止。文字は `.bb-text` を中央に重ねている。形を変えるときは hero 内の `<svg>` の path を直す。

### 次にやること
- ロイさんの合図で commit を push（メイン・シニアの2サイトをそろえて出す）。

### 動作確認
- ローカル：PC幅・スマホ幅（375px）でセクションが崩れず横スクロールなし、画像は表示（naturalWidth 240）。ライブ：push後に curl -sL で新しいHTMLと画像(200)を確認ずみ。


## 2026-09-20（日）その2 — オリーブ杯：TELの「－」や「()」を取り除いて登録（公開ずみ）

### やったこと
- 受付API `senior-site/material/worker.js` の `parseOliveEntry()`：TELを整形してから保存（全角数字→半角、ハイフン類・「ー」・丸かっこを削除）。デプロイ `0ba6d589`、commit `4f94b97`。
- `olive2-kennai.html`・`olive2-kengai.html`：送信前の `leader.tel` を同じ形に整える（確認画面にも整えた形で出る）。
- `olive2-admin.html`：`cleanTel()` を追加し、CSVのTELを `excelText(cleanTel(row.tel))` に。前から登録されていた申込みもCSVでは同じ形にそろう。
- commit `a69d149`、push済み。
- ロイさんの「OK」を受けて、スキル `apply-form-api` に登録：新規 `references/02-tel.md`（3か所の直し方・落とし穴・確かめ方）、
  ひな形 `assets/endpoints.js`・`assets/admin-page-template.html` にTEL整形と `="…"` 書き出しを入れた、`SKILL.md` に呼び出しの言葉・索引・「必ず守ること」の1行を追加。

### わかったこと・つまずいたこと
- 既存の申込みのTELは本番D1を読めなかった（自動モードの判定で止められた）ので、書き換えていない。管理ページの表には入力されたままの形で出る。

### 次にやること
- 特になし（既存の行のTELは直さない、とロイさんが決定。2026-09-20）

### 動作確認
- ✅ node：admin の CSV・worker・フォームの3か所で同じ入力8通りが同じ結果（例 `(0875)73-3458`→`0875733458`、`０９０‐１２３４−５６７８`→`09012345678`、空は空）。
- ✅ ローカル：県外用フォームで TEL に `（０８７５）７３－３４５８` を入れて確認画面へ → `0875733458` と表示、コンソールのエラーなし（送信はしていない）。
- ✅ ライブ（curl -sL）：kennai・kengai・admin の3ページに変更が入っていることを確認。
- 本番APIへの実際の申込み送信での確認はしていない。

## 2026-09-20（日）— オリーブ杯の管理ページ：CSVのTELを ="090…" の形に（Excelで0が消えない）（公開ずみ）

### やったこと
- `olive2-admin.html`：`excelText()` を追加し、`csvCell()` がそのマスを `="09028263514"` の形で書くように。
  申込み一覧（`buildCsv`）とキャンセル待ち（`buildWaitCsv`）の両方で `row.tel` → `excelText(row.tel)`。
  TELが空のときは空のマス、`"`・カンマ・改行が入っているときは式にせず従来のエスケープ。
- commit `99c350f`、push済み。

### わかったこと・つまずいたこと
- 最初はロイさんの指示どおり `""` で囲む形にしたが、それだと Excel でダブルクリックして開くと数字として読まれ、先頭の0が消える。ロイさんに2択を出して「`="…"` の形で公開」に決まった。
- CSVのファイル名は、県内・県外で絞ったときに「_香川県内」「_香川県外」がもともと付いていた（「すべて」のときは付かない）。直す必要はなかった。
- ローカルの管理ページはライブのAPIにつながる（見本表示にならない）。CSVの確認は node で `excelText`/`csvCell` を取り出して実行した。
- このPCには Excel が入っていないので、実物のExcelでは開いていない。

### 次にやること
- ロイさんが実際にCSVを保存してExcelで開き、電話番号が `090…` のまま出るか見てもらう。

### 動作確認
- ✅ node で `csvCell`：`="09028263514"`・`="0875-73-3458"`、空は空、`090,1` は `"090,1"`、他の欄は従来どおり。
- ✅ ローカルでページを開いてコンソールのエラーなし。
- ✅ ライブ（`curl -sL https://royschannel.com/olive2-admin`）に `excelText(row.tel)` が2か所あることを確認。
- Excelで開いての確認は未実施（このPCにExcelなし）。

## 2026-09-11（金）その13 — オリーブ杯の図解の取扱説明書2枚を追加／ロゴを協会の丸いロゴに

### やったこと
- 新規 `olive2-howto.html`（申込者向け）・`olive2-admin-howto.html`（協会向け）、図 `image/olive2-howto/*.png`（19枚）、ロゴ `image/kbta-logo.png`。
- `olive2-kennai.html`・`olive2-kengai.html`：取説へのリンク、ロゴ差し替え、タブのアイコン。`olive2-admin.html`：取説へのリンク。
- commit `6bd31f9`。詳細は `dev/site/kagawa-bt/DEVLOG.md` のその15。

### わかったこと・つまずいたこと
- 特になし（`renew/` と `DEVLOG.md` は今回も commit していない。`preview-howto-*.html` は gitignore の写し。図の作り直しに使うので残してある）

### 次にやること
- 特になし

### 動作確認
- ✅ ローカル：取説2枚とも画像の読み込み・横スクロール・エラーなし。
- ✅ ライブ（curl -sL）：取説2枚 200、ロゴ・図 image/png、kennai・kengai・admin に取説リンクとロゴが反映。

## 2026-09-11（金）その12 — オリーブ杯の管理ページ：キャンセル待ちの削除を申込み一覧と同じ形に（公開ずみ）

### やったこと
- `olive2-admin.html`：キャンセル待ちに行のチェック・全選択・「○件選択中」・「選択した行を削除」。行ごとの削除ボタンは廃止。commit `8f4077d`。
  詳細は `dev/site/kagawa-bt/DEVLOG.md` のその14。

### わかったこと・つまずいたこと
- 特になし（`renew/` と `DEVLOG.md` は今回も commit していない）

### 次にやること
- 特になし

### 動作確認
- `kagawa-bt/DEVLOG.md` のその14を参照（ライブ反映は `curl -sL` で確認ずみ）。

---

## 2026-09-11（金）その11 — オリーブ杯：事務局の情報を管理ページで変更／申込みページの連絡先を最新の値で表示（公開ずみ）

### やったこと
- `olive2-admin.html`：返信先の欄を「事務局の情報」（通知の届け先・返信先・担当者名・TEL・FAX）に置き換え。
- `olive2-kennai.html`・`olive2-kengai.html`：連絡先を `data-office` の span にして、開いたときに `/olive-contact` の値で置き換える。
  commit `94a82da`。詳細は `dev/site/kagawa-bt/DEVLOG.md` のその13。

### わかったこと・つまずいたこと
- 特になし（`renew/` と `DEVLOG.md` は今回も commit していない）

### 次にやること
- 特になし

### 動作確認
- `kagawa-bt/DEVLOG.md` のその13を参照（ライブの県内用ページで `/olive-contact` 200・表示一致を確認）。

---

## 2026-09-11（金）その10 — オリーブ杯の管理ページ：申込者からの返信先の欄を追加（公開ずみ）

### やったこと
- `olive2-admin.html`：送信元の欄の下に「申込者からの返信先」（`/olive-reply-to`）。commit `bebfcd4`。
  詳細は `dev/site/kagawa-bt/DEVLOG.md` のその12。

### わかったこと・つまずいたこと
- 特になし（`renew/` と `DEVLOG.md` は今回も commit していない）

### 次にやること
- 特になし

### 動作確認
- `kagawa-bt/DEVLOG.md` のその12を参照（ライブ反映は `curl -sL` で確認ずみ）。

---

## 2026-09-11（金）その9 — オリーブ杯の管理ページ：キャンセル待ちの区分切り替え・CSV保存（公開ずみ）

### やったこと
- `olive2-admin.html`：キャンセル待ちに「すべて／香川県内／香川県外」の切り替え・件数・CSV保存。「キャンセル待ちはありません。」を強調表示。
  CSVの保存処理を `saveCsvFile()` に切り出して申込み一覧と共用。commit `6f7809d`。詳細は `dev/site/kagawa-bt/DEVLOG.md` のその11。

### わかったこと・つまずいたこと
- CSVのBOMは `Blob.text()` では確認できない（取り除かれる）。`arrayBuffer()` の先頭3バイトで見る。

### 次にやること
- 特になし

### 動作確認
- `kagawa-bt/DEVLOG.md` のその11を参照（ライブ反映は `curl -sL` で確認ずみ）。

---

## 2026-09-11（金）その8 — オリーブ杯：キャンセル待ち番号での呼び出し・申込みに作り替え（公開ずみ）

### やったこと
- `olive2-kennai.html`・`olive2-kengai.html`：キャンセル待ち番号の呼び出し欄（`?w=007` で番号入り）、呼び出した内容での申込み、登録完了の画面に番号。
- `olive2-admin.html`：キャンセル待ちの一覧に番号・状態。commit `304337e`。
- 詳細は `dev/site/kagawa-bt/DEVLOG.md` のその10。

### わかったこと・つまずいたこと
- 特になし（`renew/` と `DEVLOG.md` は今回も commit していない）

### 次にやること
- 特になし

### 動作確認
- `kagawa-bt/DEVLOG.md` のその10を参照（ライブ反映は `curl -sL` で確認ずみ）。

---

## 2026-09-11（金）その7 — オリーブ杯：受付組数の上限（締切画面）とキャンセル待ちを追加（公開ずみ）

### やったこと
- `olive2-kennai.html`・`olive2-kengai.html`：開いたときに受付中かを確かめ、締切なら受付終了の画面。そこからキャンセル待ちに登録できる。
- `olive2-admin.html`：「受付組数の上限」と「キャンセル待ち」の欄。commit `14e33d4`。
- 詳細は `dev/site/kagawa-bt/DEVLOG.md` のその8。

### わかったこと・つまずいたこと
- 特になし（`renew/` と `DEVLOG.md` は今回も commit していない）

### 次にやること
- 特になし

### 動作確認
- `kagawa-bt/DEVLOG.md` のその8を参照（ライブ反映は `curl -sL` で確認ずみ）。

---

## 2026-09-11（金）その6 — 管理ページ（教室・オリーブ杯）：管理キーが通るまで、キーの欄だけを表示（公開ずみ）

### やったこと
- `bt-apply-admin.html`・`olive2-admin.html`（commit `617bd55`）
  - キーの欄より下（管理キーの変更・送信元・CC宛先・申込み一覧）を `<div id="admin-area" style="display:none;">` で包んだ。
  - 「表示する」で一覧の取得が `ok:true` のときだけ `admin-area` を表示。キー違い・空欄・通信失敗では隠す（開いたあとでも隠し直す）。
  - **キー違い（サーバーの404）を「通信に失敗しました」ではなく「キーが正しくないか、取得に失敗しました。」と出すようにした**
    （以前は `!r.ok` で例外にしていたため、キー違いでも通信失敗の文言になっていた）。
  - オリーブ杯の見本表示（`API` が空）では、キーを入れれば開く。

### わかったこと・つまずいたこと
- **オリーブ杯の管理キーが 18:16（JST）に管理ページから変更されていた**（D1の `updated_at` = 2026-09-11T09:16:36Z、9文字）。
  こちらの作業ではない。そのため `nahoko0724` では開かない。勝手に戻さず、ロイさんに確認中。
  → **協会PC用フォルダの `管理キー.txt` は `nahoko0724` のままで、今のキーと食い違っている。**
- 教室の管理キーは知らないので、キーが通ったときの動きは、ブラウザ内で `fetch` を差し替えて確かめた（本番データには触れていない）。
- 手元の表示用サーバー（site-preview）が途中で止まっていた。`preview_list` が空なら `preview_start` で起こし直す。

### 次にやること
- オリーブ杯の管理キーの扱いをロイさんに確認（今のキーのままにするか、`nahoko0724` に戻すか）→ `管理キー.txt` を合わせる。
- apply-form-api スキルの管理ページのひな形にも、同じ「キーが通るまで隠す」を入れるか検討。

### 動作確認
- ✅ 2ページとも JS 構文OK、`<div>` の開始・終了の数が一致（教室10/10、オリーブ杯12/12）。
- ✅ 開いた直後：キーの欄だけ表示（管理キーの変更・送信元・CC・一覧はすべて非表示）。
- ✅ 違うキー（本番APIに問い合わせ）：「キーが正しくないか、取得に失敗しました。」で隠れたまま。
- ✅ キーが通ったとき（fetch を差し替え）：設定・CC・送信元・一覧が表示。そのあと違うキーを入れると隠れ直す。
- ✅ ライブ：push から約1分で両ページに `id="admin-area"` が出ていることを `curl -sL` で確認。
- ⬜ 本物のキーで本番の一覧を開く動きは未確認（教室はキーを知らない／オリーブ杯はキーが変更されていた）。

---

## 2026-09-11（金）その5 — オリーブ杯の管理ページ：選手欄の「★チーム監督」を外した（公開ずみ）

### やったこと
- `olive2-admin.html`：`playerCell()` から監督の印を削除、`.mgr` のCSSも削除。commit `64c56b3`。
  詳細は `dev/site/kagawa-bt/DEVLOG.md` のその6。

### わかったこと・つまずいたこと
- 特になし

### 次にやること
- 特になし

### 動作確認
- 見本データで表示を確認、ライブ反映も `curl -sL` で確認（`kagawa-bt/DEVLOG.md` のその6）。

---

## 2026-09-11（金）その4 — オリーブ杯の管理ページの表を1行20文字で折り返し、チーム監督の列をNo.1の直前へ（公開ずみ）

### やったこと
- `olive2-admin.html`：表のマスを `<div class="c">` で包み、`width:max-content;max-width:20em` で1行20文字の折り返しに。
  「チーム監督」の列を住所のあと・No.1の直前へ移動。commit `44cb299`。詳細は `dev/site/kagawa-bt/DEVLOG.md` のその5。

### わかったこと・つまずいたこと
- td に max-width は効かない。中身の div に `width:max-content` と `max-width` の両方が要る（片方だと手前で折り返す）。

### 次にやること
- 特になし

### 動作確認
- 見本データで折り返しと列の並びを確認、ライブ反映も `curl -sL` で確認（`kagawa-bt/DEVLOG.md` のその5）。

---

## 2026-09-11（金）その3 — オリーブ杯の申込み人数に「1名」を追加（公開ずみ）

### やったこと
- `olive2-kennai.html`・`olive2-kengai.html`：申込み人数の選択肢を1〜4名に。詳細は `dev/site/kagawa-bt/DEVLOG.md` のその4。

### わかったこと・つまずいたこと
- 特になし（`renew/` と `DEVLOG.md` は今回も commit していない）

### 次にやること
- 特になし

### 動作確認
- push 後、ライブの2枚に「1名」の選択肢が出ていることを `curl -sL` で確認。

---

## 2026-09-11（金）その2 — オリーブ杯のフォームに「申込み人数（2〜4名）」を追加（公開ずみ）

### やったこと
- `olive2-kennai.html`・`olive2-kengai.html`：申込み人数の欄と、人数分だけ選手欄を出す切り替え。
  `olive2-admin.html`：「人数」の列（表・CSV）。commit `11ab7ed`。詳細は `dev/site/kagawa-bt/DEVLOG.md`。
- 県外用に「所属グラブ名」は残っていなかった（両方「所属クラブ名」）。

### わかったこと・つまずいたこと
- 特になし（`renew/` と `DEVLOG.md` は今回も commit していない）

### 次にやること
- 特になし

### 動作確認
- `kagawa-bt/DEVLOG.md` のその3を参照。

---

## 2026-09-11（金）— 香川県BT協会の「第2回オリーブ杯」申込みフォーム3枚を追加（公開ずみ）

### やったこと
- `olive2-kennai.html`（県内用）・`olive2-kengai.html`（県外用）・`olive2-admin.html`（管理）を
  `dev/site/kagawa-bt/` から**このフォルダへ移して公開**（commit `82efb70`）。受付は Worker の `/olive-*`。
- 3枚とも旧住所からの転送スクリプトを入れ、canonical はフォーム2枚だけに入れた。
- **詳しい経緯・メールの形・協会PC用フォルダは `dev/site/kagawa-bt/DEVLOG.md` に書いた。**

### わかったこと・つまずいたこと
- push 直後は Pages のビルドが終わるまで、まだ無いパスでトップページが返る。判定は `<title>` で。
- **`renew/`（トップ作り直しの下書き）と `DEVLOG.md` は今回も commit していない。**

### 次にやること
- `dev/site/kagawa-bt/DEVLOG.md` の「次にやること」を参照。

### 動作確認
- ライブの3枚が正しい title・API・canonical で開くことを `curl -sL` で確認。

---

## 2026-09-07（日）— トップページを「デザイン.md」の配色で作り直した（下書き・別フォルダ／未デプロイ）

### やったこと
- **`renew/index.html` を新規作成。** 今のトップページ（`index.html`）は一切さわっていない。
  ロイさんから渡された2ファイル（`Downloads/デザイン見本.html`・`Downloads/デザイン.md`）が下敷き。
- **中身と並び順は今のトップと完全に同じ**（ヘッダー→ヒーロー→お知らせ→3つの顔→シニア相談室→
  講座2本→プロフィール→好きなこと4本→発信→お問い合わせ→フッター）。文章も1文字も変えていない。
  変えたのは色・書体・余白・角の丸みだけ。
- **色は デザイン.md の5色だけ**に入れ替えた。

  | | 今のトップ | 作り直し版 |
  |---|---|---|
  | 背景 | `#FAF5EC` クリーム | `#FAF8F5` オフホワイト |
  | 見出し | `#1F5A72` 濃い紺 | `#4A90A4` 瀬戸内ブルー |
  | 差し色 | `#C0392B` 赤（多用） | `#8FA660` オリーブ（見出し下線・タグ・アイコン） |
  | ボタン | 紺・赤の2種類＋厚み影 | `#E89B5D` オレンジ1種類＋枠線ボタン |
  | 本文 | `#23323C` | `#3A3A3A` チャコールグレー |

- **オレンジは「押してほしいボタン」だけ**に絞った（デザイン.md のルール3）。
  2番手のボタン（「ロイってどんな人？」「Let's バウンドテニス‼」）は青の枠線だけにした。
- 書体を Google Fonts（Zen Maru Gothic／Baloo 2）から**見本と同じ端末内蔵の書体**
  （Hiragino Sans → Yu Gothic → Meiryo）に変更。外部読み込みが2本減るので表示も速い。
- 見本HTMLの**「波」の区切り**（`.wave`）をヒーローの下に採用。
- **フッターを暗い紺から明るい下地に変更**（見本のフッターに合わせた）。
- 影を弱く・角丸を10pxに統一・カードのふちを `#EDE8E0` に統一して、全体をスッキリさせた。

### わかったこと・つまずいたこと
- **置き場所は `roys-channel/renew/` にした。** リポジトリの外に置くと画像（`image/`）が
  読めず見た目を確認できないため。画像・講座ページは `../` で今あるものを共用している。
  → **push すると `royschannel.com/renew/` として公開されてしまう。** そのため
  `<meta name="robots" content="noindex">` を入れて検索には出ないようにしてある。
  正式採用するときは、この1枚を `index.html` に上書きして `renew/` を消すのが後片付けとして楽。
- **Browserパネルが隠れていると、スクロールした先のスクリーンショットが真っ白になる。**
  DOMは正常なのに「下が真っ白＝壊れている」と誤読しかけた。区画を1つずつ
  `display:none` で隠して先頭に出す方法で全区画を目視した。
- 「見られた回数」は作り直し版でもそのまま動いた（今日0回／のべ18回・3回を実際に取得）。
  集計キー（`/materials/...`）は触っていないので、数字が0に戻ることはない。

### 次にやること
- ロイさんに見てもらって、採否を決める。気になる箇所（オレンジの量・フッターの明るさ・
  書体の印象）があれば直す。
- 採用する場合：`renew/index.html` を `index.html` に上書き → push。
  そのとき `robots noindex` と冒頭のコメントを消す。
- 採用する場合、他のページ（`bt-kyoushitsu.html`・`kouza/*.html`）も同じ配色に揃えるか要相談。

### 動作確認
- **ローカルのみ（`http://localhost:8765/roys-channel/renew/`）。未デプロイ・ライブ未確認。**
- 画像26枚すべて読み込み成功（404ゼロ）。JavaScriptのエラーもゼロ。
- 全12区画をパソコン幅で目視。スマホ幅（375px）でも表示を確認、横スクロールの発生なし。
- ハンバーガーメニューの開閉を確認（9項目とも表示）。
- 講座2本の閲覧数の取得を確認。お問い合わせフォームの**送信は未テスト**（本番のWorkerに
  テストのメールを飛ばさないため）。


### 追記（同日）— 背景に模様（オリーブ・波・島影）を入れた
ロイさんのご要望「バックカラーに合わせて、波・山・オリーブの木をモチーフにした簡素な模様」を反映。

- **模様は画像ファイルではなく、CSSの中に描いた線画（SVG）。** `:root` に4つ用意した。
  読み込むファイルが増えないので表示は遅くならず、どれだけ拡大しても粗くならない。

  | 変数 | 絵 | 使っている場所 |
  |---|---|---|
  | `--pat-olive` | オリーブの葉と実（190pxの繰り返し） | `body`＝ページ全体の下地 |
  | `--pat-islands` | 瀬戸内の島影（手前・奥の2枚重ね） | `.wave`＝ヒーローの下の区切り |
  | `--pat-wave` | 波（横一列の繰り返し） | `.notice`・`.senior`・`.contact` の下ぎわ |
  | `--pat-branch` | オリーブの枝1本 | `.kouza.alt`（Claude入門）の右上 |

- **色は5色のまま。**うすさ（opacity 0.10〜0.16）だけで「うっすら模様がある」程度に抑えた。
- **青い区画は「海」に見立てた。**島影のふもとが、すぐ下の青い区画の水ぎわに重なるように置いている。
  波は青い区画の下辺に並べて、なぎさに見せた。
- 模様を全部やめたいときは、`body` / `.wave` / `.notice` / `.senior` / `.contact` / `.kouza.alt` の
  `background-image` を消せばよい（CSSにその旨のコメントを入れてある）。
- **無地版は `renew/plain-backup.html` に残してある。** 模様あり／なしを見比べられる。

#### わかったこと・つまずいたこと
- 最初、葉の形が「魚か目」に見えた。**葉の真ん中に背景色で1本すじ（葉脈）を入れると、
  一気に葉に見える。** 大きさは170px→190pxに広げ、うすさも0.13→0.11に落として控えめにした。
- オリーブの枝を `background-position:right -34px` で置いたら、枝の先だけが切れて見えて
  「枝」に見えなかった。`right 18px` に直して全体を見せた。
- **`.likes`（好きなこと）は白のまま、模様を入れていない。** 全部に模様を敷くと目が休まらないため。

#### 動作確認（追記ぶん）
- ローカルのみ。**未デプロイ・ライブ未確認。**
- 4つの模様がすべて適用されていることをCSSの計算値で確認。画像404ゼロ・JSエラーゼロ。
- パソコン幅（1280px）とスマホ幅（375px）の両方で、島影・波・葉・枝を目視。横スクロールなし。
- スマホでは枝を190pxに縮小して、文字にかぶらないようにした（確認ずみ）。


### 追記（同日・その2）— `renew/design.md` を作成
作り直し版で実際に使っている決まりごとを1本にまとめた。全10章。

- 中身：コンセプト／カラーパレット（5色＋派生9つ）／背景の模様4つ／文字／部品（丸み・ボタン・
  見出し・カード・影）／余白／レスポンシブ／トーン／NG例／どのファイルのどこを直すか
- **数字はぜんぶ実物のCSSから拾って書いた。**記憶で書かない。
- 書いたあとに実物と突き合わせて、**4か所の食い違いを見つけて直した**：
  1. `--accent-deep` は「ボタンのホバー」だけでなく `.motto b` でも使っている
  2. 「ホバーは色が変わるだけ」と書いたが、実際は**うすいオレンジのぼかし影も出る**
  3. 「影は `--shadow` 1種類だけ」と書いたが、ボタンのホバーにもう1つある
  4. 「丸みは 10px / 8px / 999px 以外を使わない」と書いたが、実際は
     **12px（クロの吹き出し）・6px（アプリのサムネイル）の例外がある**
- `--main-deep`（`#3B7788`）は**どこにも使っていない**ことを確認（0件）。「予備」と明記した。

#### わかったこと・つまずいたこと
- **デザインガイドこそ、書いたあとに実物と照合しないと嘘になる。**
  今回は「使っていない色」「無いはずの影」「守れていない丸みのルール」が
  自分の書いた文章の中に4つ出た。書いた直後に grep で全部当たるのが早い。
- ルールを書くときは「例外なし」と言い切りたくなるが、**実物に例外があるなら
  例外の表を作って数を固定する**ほうが守られる（「これ以上ふやさない」と書ける）。

#### 動作確認（追記ぶん）
- `design.md` に書いた色・数値・クラス名を、`index.html` に grep で照合ずみ。
- ページ自体は変更していない（`design.md` の追加のみ）。**未デプロイ。**

---


## 2026-09-06（日）— 講座を royschannel.com/kouza/ に集約し、Claude入門（全18回）を公開

### やったこと
**1. 「田舎暮らしのまま、独りで稼ぐ教科書」をシニアサイトから引っ越した。**
ロイさんの指示（「it.royschannel.com の下から royschannel.com の下へ移したい」）。

| | 前 | 後 |
|---|---|---|
| ページ | `it.royschannel.com/material/inaka-hitori-kasegu` | **`royschannel.com/kouza/inaka-hitori-kasegu`** |
| 動画7本 | `senior-site/videos/kouza_ep*.mp4` | **`roys-channel/videos/kouza_ep*.mp4`** |
| 旧URL | — | senior-site の **`_redirects` で 301 転送** |

- ページの中で直したのは4か所：canonical／旧住所からの転送スクリプト（`roys-channel.pages.dev` 側へ）／
  アクセス計測の `LIVE_HOSTS`／画像のパス（`../image/kuro/…` → `../image/…`、`profile-photo.jpg` → `photo-profile.jpg`）
- **閲覧数の集計キー（`/materials/inaka-hitori-kasegu.html`）は変えていない。**
  変えると、これまでの「見られた回数」がゼロから数え直しになるため
- `index.html` の講座ボタンを相対リンク `kouza/inaka-hitori-kasegu` に変更

**2. 「Windowsユーザーのための Claude 入門」（全18回）を新しく公開した。**

- `kouza/claude-nyumon.html` … 新規。**見た目は隣の田舎暮らし講座と同じCSSを使い回した**
  （生成スクリプトは `scratchpad/gen_claude_page.py`。回のデータから目次・カード18枚を組み立てる）
- 動画18本 `videos/claude_ep0〜17.mp4`（**`完成_ご案内つき\` のほう**＝末尾に問い合わせ案内21.5秒つき）＋
  poster 18枚 `claude_ep*.jpg`（各動画の6.0秒地点＝題名が出ている場面）
- 表紙 `image/cover-claude.jpg`（第0回の題名の板を切り出したもの）
- `index.html` に講座欄をもう1つ追加（`#kouza-claude`・`.kouza.alt`）。ナビにも「Claude入門」を追加（PC・スマホの2か所）
- トップの「見られた回数」を**2本ぶんに一般化**（`KOUZA_COUNTERS` の対応表で回す形に）

**3. `site-kit/references/01-architecture.md` を現状に合わせて直した**（`kouza/`・`videos/`・`_redirects`）。

### わかったこと・つまずいたこと
- **★第0回〜第5回の動画に、古い名札「全10回の入門講座」が焼き込まれたまま残っている。**
  講座は全18回になったのに、題名の板の上のバッジだけ昔の数字。第6回以降は「第2部 Cowork編」等なので問題なし。
  サイトの表紙は、この帯を**切り落として**作った（`crop=1760:600:84:245`）が、**動画の中身は直っていない**。
  直すには `parts/ep01〜05_body.html` と `ep00/index.html` の1行を変えて**6本を書き出し直す（約1時間）**。
  読み上げ・字幕は変わらないので**音声の作り直しは不要**（第16回のときと同じ）
- **`_redirects` は改行コードに注意。** Windowsのgitは checkout時にCRLFへ変える設定なので、
  `.gitattributes` に `_redirects text eol=lf` を書いて固定した（リポジトリ内はCRなしを確認）
- **ナビへの追記で、6字下げの行に4字下げのパターンが部分一致した。**
  PC用ナビに余計な行が1本入り、スマホ用には入らないという壊れ方をした。**行頭の空白まで含めて照合すること**
- 画面が隠れていると、ブラウザのスクリーンショットは**スクロールしても中身が出ない**（描画が止まるため）。
  縦長のビューポート（例 1000×5600）にして**一度に全体を撮る**と見られる

### 追記（同日 20:30）— 第0〜5回の名札を直して差し替えた
ロイさんの指示で、古い名札「全10回の入門講座」を直し、**6本を書き出し直して差し替えた**。
第0回＝「全18回の入門講座」、第1〜4回＝「第1部　チャット編」、第5回＝「第1部　チャット編　最終回」
（第2部・第3部の書き方にそろえた）。動画の長さ・形は前と同じで、**絵だけが変わっている**。
サムネイル6枚も作り直した。詳しくは `dev/tool/claude-nyumon-slides/DEVLOG.md`（同日その5）。

### 次にやること
- ロイさんの試聴・目視
- note などに貼った旧URLは301で飛ぶが、**貼り替えられるものは新URLに直すと確実**

### 動作確認
- ローカル（`http://localhost:8765`）… 引っ越した田舎暮らし講座＝画像4枚・動画7本・poster7枚とも **200**。
  Claude入門＝動画18本・poster18枚・画像4枚とも **200**、目次18本のアンカーは**全部つながる**、
  取りこぼしの差し込み文字（`TOTAL_MIN` 等）**0件**
- トップ … 新しい講座欄が表示され、ナビは9項目で崩れないことを画面で確認（PC幅800pxでも収まる）
- **ライブ（royschannel.com）でも確認ずみ**（push → Cloudflare Pages 自動ビルド）
  - `/kouza/claude-nyumon`・`/kouza/inaka-hitori-kasegu` … どちらも 200 で正しい `<title>`
  - 動画18本＋poster18枚（`claude_ep*`）… **36ファイルすべて 200**（`video/mp4`）
  - 田舎暮らしの動画7本（`kouza_ep*`）… **7本すべて 200**
  - トップの講座ボタン … `kouza/claude-nyumon`・`kouza/inaka-hitori-kasegu` の2本が出ている
  - 表紙・クロ・プロフィール写真 … 5枚とも 200

## 2026-09-02（水）その2 — 申込みフォーム一式の作り方をスキル化（apply-form-api）

### やったこと
ロイさんの依頼で、今回作った申込みフォームを**別のシーンで作り直せる形**に残した。

**新スキル `apply-form-api`**（`~/.claude/skills/apply-form-api/`）

| ファイル | 中身 | 行数 |
|---|---|---|
| `SKILL.md` | 0〜8段の手順＋「必ず守ること」の表＋動作確認の型 | 178 |
| `references/01-mail-setup.md` | Resendの独自ドメイン認証・DMARC・送信元の決め方・**届かないときの調べ方** | 156 |
| `assets/endpoints.js` | Workerに貼り足すエンドポイント一式 | 542 |
| `assets/admin-page-template.html` | 申込み一覧の管理ページ | 651 |
| `assets/schema.sql` | D1のテーブル4つ | 45 |

**ひな形は手書きせず、実際に動いているコードから機械的に取り出して固有名詞を置換した**
（`worker.js` → `endpoints.js`、`bt-apply-admin.html` → `admin-page-template.html`）。
`BT_` `bt_` `/bt-` 「バウンドテニス」の残りが0件であることを確認し、`node --check` も通した。

**入力フォームのひな形は作らなかった。** site-kit に `assets/form-page-template.html` が
既にあるため。**同じものを2つ持たない**（`01-architecture.md` の方針に従う）。

### site-kit 側も現状に合わせて直した
今日の変更で**site-kit の記述が古くなった箇所**があったので、あわせて修正。

- `SKILL.md` … 索引に `apply-form-api` への行を追加。作業の型Aにも導線を1行
- `07-forms.md` … **`DELETE /bt-applications?key=...` の記述が古くなっていた**ので修正。
  「管理キーをURLに載せない」の節を新設（CORSプリフライトの確認手順つき）
- `03-backend-api.md` … 「CC・受付完了メールを実際に届くようにするには（未実施）」を
  **「2026-09-02 に有効化ずみ」**に更新。`ok:true` はCC成功の証明にならないこと、
  「配達済みなのにどこにも無い」は受信側の転送を疑うことを追記

### わかったこと・つまずいたこと
- **置き場所はロイさんの過去の裁定と衝突しかけた。** 2026-08-31に「サイトのノウハウは
  site-kit に統合する（独立スキルにしない）」と決めている。今回は「スキル登録して」という
  明示の依頼だったので、**独立スキルとして作り、site-kit の索引からも辿れるようにした。**
  `senior-site-material` と同じ形（本体はsite-kit、固有の情報だけ別スキル）。
- **`SKILL.md` の frontmatter を触ったら `yaml.safe_load` で検査する。**
  2026-08-31 に一度壊してスキル一覧の description が化けた実績がある。
  今回は新旧2つとも検査ずみ（apply-form-api 592文字／site-kit 652文字・変化なし）。
- 相対リンクは両スキルとも全件走査して**リンク切れ0件**。

### 次にやること
- 特になし。次に申込みフォームを作るときに、このスキルが実際に使えるかを見る。

### 動作確認
- 両スキルの frontmatter が `yaml.safe_load` で読めることを確認。
- **スキル一覧に完全な description が表示されることを実機で確認**（`apply-form-api` が
  一覧に出て、site-kit も従来どおり表示されている）。
- `assets/endpoints.js` … `node --check` OK。固有名詞の残り0件。
- 相対リンク … apply-form-api・site-kit とも切れ0件。
- **ひな形を使って実際にフォームを1本作る検証は未実施**（次の機会に確かめる）。

## 2026-09-02（水）— 管理ページの管理キーがWorkerのログに平文で残っていたのを直した

### やったこと
`bt-apply-admin.html` の GET/DELETE 5か所を、**管理キーをURLの `?key=` ではなく
`X-Admin-Key` ヘッダーで送る形**に変更（commit `c5eaf18`・push・ライブ確認ずみ）。

| 直した箇所 | 前 | 後 |
|---|---|---|
| CC宛先の一覧（GET） | `/bt-cc-recipients?key=…` | `/bt-cc-recipients` ＋ヘッダー |
| CC宛先の削除（DELETE） | `?key=…&id=…` | `?id=…` ＋ヘッダー |
| 送信元アドレスの取得（GET） | `/bt-mail-from?key=…` | `/bt-mail-from` ＋ヘッダー |
| 申込みの削除（DELETE） | `?key=…&ids=…` | `?ids=…` ＋ヘッダー |
| 申込み一覧の取得（GET） | `/bt-applications?key=…` | `/bt-applications` ＋ヘッダー |

POSTの3か所（CC追加・管理キー変更・送信元変更）は**元からリクエストボディで**送っており、
URLには出ないのでそのまま。

### わかったこと・つまずいたこと
- **★実際に漏れているのを目で見た。** バウンドテニスのCC不着を調べるため `wrangler tail` を
  流していたところ、ロイさんが管理ページを操作した瞬間に
  `GET /bt-cc-recipients?key=（管理キーそのもの）` がログに出た。
  **URLはWorkerのログに丸ごと残る。秘密をURLに載せてはいけない。**
- **Worker側は以前から `X-Admin-Key` ヘッダーを受け付ける作りだった**
  （`request.headers.get("X-Admin-Key") || url.searchParams.get("key")`）。
  だから**直したのはHTMLだけ。Workerのデプロイは不要。**
- **ヘッダーを足すとCORSのプリフライトが走る。** 適用前に実際に OPTIONS を投げて
  `Access-Control-Allow-Headers: Content-Type,X-Admin-Key` が返ることを確認してから直した。
  ここが許可されていなければブラウザ側で弾かれて管理ページが動かなくなる。
- 採取したログファイルは削除し、作業フォルダ全体を検索して管理キーの残留が無いことを確認した。

### 次にやること
- **ロイさんが管理キーを変更する。** 今日のログに載ってしまったので、管理ページの
  「管理キーの変更」から新しい値にする。**テストデータの削除を先に済ませてから**変える方が手数が少ない。
- バウンドテニス教室のテスト申込み4件（「テスト太郎」〜「テスト太郎4」）を管理ページから削除する。

### 動作確認
- `node --check` で、ページ内の全 `<script>` の構文がOK。
- ローカル（`site-preview` 8765）で管理ページを開き、**コンソールエラー0件**を確認。
- **ライブの生HTMLで確認ずみ。** `curl -sL` で取得し、`adminHeaders` が6回（定義1＋使用5）、
  **古い `?key=' + encodeURIComponent(key)` の形が0件**であることを確認。
- 管理キーを使った実際のログイン動作は**ロイさんの確認待ち**（キーを持っていないため未実施）。

## 2026-08-31（日）その4 — バウンドテニス2ページの転送漏れを解消／og:image を絶対URLに

### やったこと（commit `bf3d918`・push・ライブ確認ずみ）
1. **`bt-kyoushitsu.html`（申込みフォーム）**
   - 旧住所からの転送スクリプトを追加（`index.html` と同じ形。旧2ホスト対応＋先頭スラッシュ落とし）
   - `<link rel="canonical" href="https://royschannel.com/bt-kyoushitsu">` を追加（`.html` 無し）
2. **`bt-apply-admin.html`（管理ページ）**
   - 転送スクリプトのみ追加。**canonical は入れない**
     （`noindex,nofollow` のページに正規URLを宣言する意味がなく、むしろ紛らわしい）
3. **`index.html`**
   - `og:image` を相対パス `image/roy-chara-side.jpg` から
     `https://royschannel.com/image/roy-chara-side.jpg` へ変更

### わかったこと・つまずいたこと
- **この2ページには以前から転送も canonical も入っていなかった。**
  `site-kit/references/01-architecture.md` の「全ページの `<head>` の先頭に」という記述が
  この2ページについて正しくなかった。**新しいページを作ったら転送スクリプトを入れ忘れない。**
  同ドキュメントを現状（旧2ホスト対応・スラッシュ落とし・この落とし穴）に合わせて更新した。
- **`og:image` は絶対URLで書く。** 相対パスだとSNSのクローラが画像を拾えない。
  シニアサイトのOGP作業で同じ論点に当たり、こちらにも同じ問題が残っていたのを見つけた。
- 挿入した転送ブロックのJSは **node で構文検査してから** commit した
  （正規表現の `\/` を含むので、書き込み時のエスケープ崩れを検知するため）。
- `bt-kyoushitsu.html`・`bt-apply-admin.html` は **LF**、`index.html` は **CRLF**。
  `io.open(newline='')` で各ファイルの改行を保った（差分は追記のみ：14行／13行／1行）。
- ブラウザから `*.pages.dev` へのアクセスが途中から拒否されるようになったため、
  **旧URLからの転送はブラウザで実測できなかった。** 代わりに旧URLの生HTMLを `curl` で取得し、
  転送コードが配信されていることを確認した（同じコードでの転送動作は
  senior-site 側で実測ずみ）。

### 次にやること
- `index.html` に `og:url` が無い（`og:title`・`og:description`・`og:type`・`og:image` はある）。
  足すなら1行。急ぎではない。
- `bt-kyoushitsu.html` に `og:image` が無い（`og:title`・`og:description` はある）。
  SNSに貼ったときカード画像が出ない。
- `www.royschannel.com` → apex への Redirect Rule（今も `www` で普通に開けるので必須ではない）

### 動作確認
- ローカル（`http://localhost:8765/roys-channel/bt-kyoushitsu.html`）… localhost では転送せず、
  canonical は新ドメイン、**フォーム16項目**と案内ポスター（900×1282）が無事、壊れた画像0件。
  管理ページも転送せず、canonical は0件・`robots` は `noindex,nofollow` のまま。
- **ライブ確認ずみ。**
  - `https://royschannel.com/bt-kyoushitsu` … 転送されず正常表示（無限ループなし）、
    canonical `https://royschannel.com/bt-kyoushitsu`、フォーム16項目、壊れた画像0件
  - 旧URL `roys-channel.pages.dev/bt-kyoushitsu` と `/bt-apply-admin` の生HTMLに
    転送コード（`indexOf(location.hostname)`・`'roys-channel.pages.dev'`・
    `replace(/^\//, '')`）が配信されていることを `curl` で確認
  - `https://royschannel.com/` の `og:image` が絶対URLになっていることをブラウザで確認。
    その画像は `Content-Type: image/jpeg` / `Content-Length: 158377` で配信されている
- **未実測**：旧URLを実際に開いたときの転送動作（ブラウザ側の制限のため）。
  ロイさんの手元で `https://roys-channel.pages.dev/bt-kyoushitsu` を開くと確認できる。
- **未実測**：SNSに貼ったときのカード表示。


## 2026-08-31（日）その3 — 旧 pages.dev から新ドメインへの転送を有効化（移行の2段目）

### やったこと
- `index.html` の転送スクリプトを2箇所変更（commit `968a0f0`・push ずみ）。
  - hostname の判定を配列に拡張：`['roys1105.github.io', 'roys-channel.pages.dev']`
  - `rest` の先頭の `/` を落とす `.replace(/^\//, '')` を追加（下記のバグ対応）
- これで `roys-channel.pages.dev` を開いた人も `royschannel.com` へ送られる。

### わかったこと・つまずいたこと
- **★適用前に机上検算をしてバグを見つけた。** 転送先を組み立てる行は
  `location.pathname.replace(/^\/roys-senior-it\/?/, '')` で旧 GitHub Pages の接頭辞を
  剥がしていた。**pages.dev には接頭辞が無いので剥がれず、`rest` の先頭に "/" が残る。**
  結果 `'https://it.royschannel.com/' + '/tokushoho.html'` で **`//` になっていた**。
  7ケースを机上で並べたら **4件が不正URL**。`.replace(/^\//, '')` を後ろに足して解決。
  → **転送の書き換えは、必ず全パスのパターンを並べて検算してから適用する。**
- **ホスト名の囲みは絶対に外さない。** 外すと新ドメイン上でも転送が走り、無限ループになる。
  適用前に、各ホスト名で判定がどうなるかを一覧にして確認した
  （新ドメイン・localhost は「転送しない」／旧2ホストのみ「転送する」）。
- senior-site のファイルには `indexOf(location.hostname)` が**2箇所**出る。
  1つは転送の判定、もう1つはアクセス計測の `LIVE_HOSTS.indexOf(...)`。別物なので混同しない。
- **★`roys-channel/bt-kyoushitsu.html` と `bt-apply-admin.html` には、
  転送スクリプトも canonical も入っていない**（以前から）。
  そのため旧URL `roys-channel.pages.dev/bt-kyoushitsu` は**新ドメインへ転送されない**
  （実測で確認）。旧URLのままでもフォームは動く（WorkerのCORSは `*`）が、住所は一本化されていない。
  `site-kit/references/01-architecture.md` の「全ページの `<head>` の先頭に」という記述は
  この2ページについては正しくない。**未対応。**

### 次にやること
- `bt-kyoushitsu.html`・`bt-apply-admin.html` に転送スクリプトと canonical を入れるか判断する
- `og:image` が相対パス（`image/roy-chara-side.jpg`）のまま。絶対URLに直す

### 動作確認
- ローカルで、各ホスト名の判定を一覧にして確認。
  新ドメイン・`www`・localhost は「転送しない」／旧2ホストのみ「転送する」。
- **ライブ確認ずみ。**
  - `https://royschannel.com/` … 転送されず正常表示（無限ループなし）
  - `https://roys-channel.pages.dev/bt-kyoushitsu` … **転送されない**（このページに
    スクリプトが無いため。上記の積み残し）
- push から反映まで約10秒。

## 2026-08-31（日）その2 — バウンドテニス教室の案内ポスターがライブで表示されていなかった

### やったこと
- ロイさんから「案内ポスターが表示されない」と相談。原因は**画像2枚が未コミット**だった。
  2026-08-16 にローカルの `image/` に置いたまま `git add` しておらず、Cloudflare Pages の
  ビルドに渡っていなかった（`git status` では `??` のまま）。
- 2枚をコミットして push（commit `b60b1f5`）。
  - `image/bt-kyoushitsu-flyer-kari.jpg`（256,815バイト）… `index.html` と `bt-kyoushitsu.html` で参照
  - `image/bt-good-work.png`（222,653バイト）… `bt-kyoushitsu.html` で参照
- あわせて `.wrangler/`（wranglerの作業用キャッシュ）を `.gitignore` に追加。
  これで roys-channel の未コミットは0件になった。

### わかったこと・つまずいたこと
- **★Cloudflare Pages は存在しないファイルにも HTTP 200 を返す。** 中身は404ページのHTMLで、
  `Content-Type: text/html; charset=utf-8` になっている。**ステータスコードだけ見ると
  「ファイルはある」と誤判定する**（今回、一度そう判定しかけた）。
  正しい判定は `Content-Type`：

  ```bash
  curl -s -o /dev/null -D - "https://royschannel.com/image/xxx.jpg" | grep -i "content-type"
  ```

  - ある … `Content-Type: image/jpeg` ＋ `Content-Length` が付く
  - 無い … `Content-Type: text/html`（`Content-Length` が無い）

  この判定方法を `site-kit/references/11-verify.md` と `12-pitfalls.md` に書き足した。
- **予防**：画像を足したら、HTMLを直すのと同じ commit に画像も入れる。
  `git status` に `??` の画像が残っていたら、それは本番に出ていない。
- ブラウザのスクリーンショットが真っ白しか撮れず、目視での証拠は残せなかった。
  代わりに JS で `naturalWidth` / `complete` を読んで確認した。

### 次にやること
- 特になし（この件は完了）。

### 動作確認
- **ライブ確認ずみ。** `royschannel.com/image/bt-kyoushitsu-flyer-kari.jpg` が
  `Content-Type: image/jpeg` / `Content-Length: 256815`（ローカルのファイルサイズと完全一致）。
  `bt-good-work.png` も `image/png` / `222653` で一致。
- 旧URL `roys-channel.pages.dev` 側も同じビルドなので直っていることを確認。
- ブラウザ（`https://royschannel.com/`）で当該 `<img>` を JS で調べ、
  `naturalWidth=900` `naturalHeight=1282` `complete=true`、
  **ページ内の壊れた画像は0件**であることを確認。
- push から反映まで約20秒。


## 2026-08-31（日）— 独自ドメイン royschannel.com へ移行（内部URLの書き換え・公開ずみ）

### やったこと
- `royschannel.com`（お名前.com取得）を Cloudflare に接続し、DNSを Cloudflare に移した。
  Pages プロジェクト `roys-channel` に `royschannel.com` と `www.royschannel.com` を
  カスタムドメインとして追加（どちらもアクティブ・SSL有効）。
- `index.html` の旧URL9箇所を書き換え（commit `c353d1d`・push ずみ）。
  - canonical → `https://royschannel.com/`
  - 旧住所（github.io）からの転送先 → `https://royschannel.com/`
  - シニアサイトへの相互リンク4本 → `https://it.royschannel.com/...`
  - コメント2箇所の記述
- 書き換えは `dev/tool/domain-migration/migrate_domain.py` で実施（dry-run→apply の2段構え）。

### わかったこと・つまずいたこと
- **`LIVE_HOSTS` を見落とすと訪問数が0になる。** senior-site の5ファイルに
  `var LIVE_HOSTS = [...]` があり、ここに載っているホスト名でしか計測しない。
  **新旧の両方を入れた**（旧 pages.dev も開けたままなので、片方だけだと取りこぼす）。
- **`LIVE_VISIT_PAGES`（数える側）は直す必要が無かった。** 記憶に「2か所とも直す」とあったので
  確認したが、中身は**パスの配列**でホスト名を含まない。新ドメインでもトップのパスは `/` のまま
  （送る側は `page: location.pathname || "/"`）。
- **旧 pages.dev → 新ドメインの転送は、あえて別の段（2段目）にした。** 1回のpushで一緒に入れると、
  独自ドメインの設定が効いていなかったときに旧URLも新URLも両方開けなくなる。
- Cloudflare の画面の文言が変わっていた：「Add a domain」→「サイトを追加」→「**ドメインを接続**」。
  お名前.com も「ドメイン」ではなく「**ネームサーバー / DNS**」メニューの下だった。
- **Pages プロジェクトはドメイン画面の検索窓では出てこない。** 左上のロゴでアカウントホームに
  戻り「Workers」の「>」から。`wrangler` に `pages domain` サブコマンドは無い（4.123.0）。
- お名前.com 由来の **NSレコード22件**（各サブドメインを `*.onamae.com` に委任）がスキャンで
  見つかったので削除した。`it` の委任が残っていると `it.royschannel.com` が使えなくなる。

### 次にやること
- 旧 `pages.dev` から新ドメインへの転送を有効化（`migrate_domain.py --stage 2 --apply` → push）
- `www.royschannel.com` → apex への Redirect Rule を Cloudflare で1本
- note・YouTube固定コメントに貼った旧URLの差し替え（手作業）

### 動作確認
- ローカル（`http://localhost:8765/roys-channel/index.html`）… canonical が新ドメイン、
  シニアサイトへのリンク4本すべて `it.royschannel.com`、`pages.dev` のリンクは0本。
  localhost では転送が発動しないことも確認。
- **ライブ確認ずみ。** `curl -sL` で `https://royschannel.com/`・`https://www.royschannel.com/`・
  `https://royschannel.com/bt-kyoushitsu` すべて HTTP 200、生HTMLに `pages.dev` が**0件**。
- 旧 `https://roys-channel.pages.dev/` も HTTP 200 で開ける（2段目の前なので正常）。

## 2026-08-18（火）その9 — バウンドテニスボタンの文言を微調整

### やったこと
- ボタン文言「Let's バウンドテニス‼!」の末尾の半角「!」を削除し、「Let's バウンドテニス‼」に。
  （全角の「‼」はそのまま）

### 動作確認
- ライブでボタンのテキストが「Let's バウンドテニス‼」になっていることを確認。

---

## 2026-08-18（火）その8 — バウンドテニスYouTubeリンクをボタン形式に

### やったこと
- その7で追加した「Let's バウンドテニス‼! https://www.youtube.com/@BTC-Kagawa」のテキストリンクを、
  URLの直接表示をやめて「Let's バウンドテニス‼!」のみの表示にし、既存の`.btn.btn-red`
  （お知らせセクションの「参加申込みフォームへ」等と同じ、ホバーで浮き上がる赤ボタン）に変更。

### 動作確認
- ライブ（`https://roys-channel.pages.dev/`）で、ボタンのクラスが`btn btn-red`、
  リンク先が`https://www.youtube.com/@BTC-Kagawa`のままであることを確認。

---

## 2026-08-18（火）その7 — バウンドテニス紹介文の改行崩れ修正・YouTubeリンク追加

### やったこと
- 「好きなこと」セクションのバウンドテニス紹介文（`#bt`）で、「「さぬきうどん杯」もそのひとつです。」
  の文末（「す。」）が次行に孤立していた（その4・その6と同じ症状）。該当フレーズが container幅422pxに
  対し必要幅243pxで収まることを確認したうえで、`<span style="white-space:nowrap;">`で囲んで解消。
- 同じ紹介文の下に「Let's バウンドテニス‼! https://www.youtube.com/@BTC-Kagawa」のリンクを追加
  （ロイさんの指示。香川県のバウンドテニスチャンネルへの外部リンク）。

### 動作確認
- ローカルで、該当spanの`getClientRects().length`が1（＝1行）であることを確認。
- ライブ（`https://roys-channel.pages.dev/`）でリンクの表示・改行修正の両方を確認。

---

## 2026-08-18（火）その6 — 「MAIN WORK」紹介文、一文全体を1行に

### 起きていたこと
その5の対応後、ロイさんから「「今さら聞けない」を〜解説しています。までが1行で表示されるべきでは」
と再指摘。実測したところ、段落幅（`.senior-lead`のmax-width:34em=561px）に対し、
その一文をつなげると594pxが必要で、33px足りず2行に割れていた。

### やったこと
- ロイさんに3案（幅を広げる／文字を小さくする／2行のまま）を確認いただき、「幅を広げる」を選択。
- `.senior-lead`の`max-width`を`34em`→`37em`（561px→610.5px）に変更。

### 動作確認
- ローカルで、Rangeオブジェクトの`getClientRects()`が全て同じ`top`値になること（＝1行）を確認。
- ライブ（`https://roys-channel.pages.dev/`）でも`max-width`が610.5pxに反映されていることを確認。

---

## 2026-08-18（火）その5 — 「MAIN WORK」紹介文に指定の改行を追加

### やったこと
- 「でまるごと解説しています。」の後、「無料で使える脳トレアプリもご用意しました。」の前に
  ロイさん指定の`<br>`を追加。「でまるごと解説しています。」自体は文が長く、幅561px程度の
  コンテナでは単独でも折り返しが起きる（前回その4の`nowrap`指定が無いと「す。」だけ孤立する）ため、
  `nowrap`指定はそのまま残し、その後ろに`<br>`を足す形にした。

### 動作確認
- ライブ（`https://roys-channel.pages.dev/`）で、該当段落のHTMLに`<span style="white-space:nowrap;">
  でまるごと解説しています。</span><br>`の順で入っていることを確認。

---

## 2026-08-18（火）その4 — 「MAIN WORK」セクションの改行崩れを修正

### やったこと
- `index.html`の「電源の入れ方から〜」の紹介文で、「でまるごと解説しています。」の
  「す。」だけが次行に孤立していた（ロイさんがスクリーンショットで指摘）。
  該当フレーズを`<span style="white-space:nowrap;">`で囲み、途中で改行されないようにした。
  デプロイ・pushして反映。

### 動作確認
- ローカルで`getClientRects().length`が1であることを確認（＝1行にまとまっている）。
- ライブ（`https://roys-channel.pages.dev/`）でも同様に確認。

---

## 2026-08-18（火）その3 — トップページに「お知らせ」セクションをデプロイ

### やったこと
- 前回（その2）で見つかっていた`index.html`の未コミット分（バウンドテニス教室のお知らせセクション、
  ヘッダーメニューへの「お知らせ」リンク追加）を、ロイさんに内容確認いただいたうえでコミット・push。
  Cloudflare Pagesに自動反映。

### 動作確認
- `https://roys-channel.pages.dev/`をライブで確認。ヘッダーメニューに「お知らせ」リンクが表示され、
  `#notice`セクション（お知らせバッジ・見出し・案内文・「参加申込みフォームへ」ボタン）が
  正しく表示されることを確認。

---

## 2026-08-18（火）その2 — 管理ページの「未コミットのまま溜まっていた機能」をデプロイ

### 起きていたこと
ロイさんから「このページ（bt-apply-admin.html）が変更されている。以前は確認キーの変更やCC登録機能が
あったはずだ」と指摘があった。確認したところ、**管理キー変更・CC宛先管理・送信元アドレス変更・
一括削除・CSV書き出しの機能はローカルには実装済みだったが、一度もgitコミットされておらず、
本番（Cloudflare Pages）には反映されていなかった**。前回セッションの区切り時にコミットし忘れていたと思われる。
サーバー側（`material/worker.js`の`/bt-admin-key` `/bt-cc-recipients` `/bt-mail-from`）は
既に本番稼働ずみだったため、画面側だけが古いままになっていた。

### やったこと
- `bt-apply-admin.html`の差分（460行）を内容確認のうえコミット・push。Cloudflare Pagesに自動反映。

### わかったこと・つまずいたこと
- **このリポジトリでは「gitに未コミットのまま、ファイルの中身だけ更新されている」状態が起こりやすい。**
  作業の区切りごとに`git status`で未コミットの変更が残っていないか確認したほうがよい。
- `index.html`（トップページ）にも、バウンドテニス教室のお知らせセクションを追加する未コミットの
  変更が残っている（今回は対象外・ロイさんに要確認）。
- `image/bt-good-work.png`・`image/bt-kyoushitsu-flyer-kari.jpg`はgit履歴に一度も記録がないが、
  ライブでは200が返る（＝画像自体は別経路で本番に存在する）。gitと本番の状態が完全には一致していない
  可能性があるため、心当たりのない差分に気づいたときは、まずライブで実際に何が表示されているか
  確認するのが確実。

### 動作確認
- `https://roys-channel.pages.dev/bt-apply-admin.html`をライブで確認。「管理キーを変更する」
  「申込みメールの送信元アドレス」「申込み通知メールのCC宛先」が表示されることを確認。

---

## 2026-08-18（火）— 申込みフォームの「任意」項目チェックを撤廃・エラー表示を改善・対象欄に注記追加（未デプロイ）

### やったこと
1. **`bt-kyoushitsu.html`**：「メールアドレス」「郵便番号」の形式チェック（`@`が無い／7桁でない等で
   エラーにしていた処理）を削除。任意項目は、空欄でも・多少形式が崩れていても、確認画面へ進める
   ようにした。ロイさんから「任意の項目が未入力なのにエラーになる」という報告があり、実際に再現テスト
   したところ（必須項目のみ入力・任意項目は空欄）エラーは出なかったため、原因の特定はできなかったが、
   「任意項目は一切チェックしない」という明確な形にすることで、どんな状況でも起こり得ないようにした。
2. **エラーメッセージの見た目を強化**：`.err`のCSSを、赤文字＋太字（900）＋背景色つきの目立つ
   カード形状に変更（`⚠`アイコン付き）。文言も「「お名前」が入力されていません。ご記入をお願いします。」
   のように、どの項目のことか・何をすればよいかが分かる言い回しに統一。
3. **`material/worker.js`**（サーバー側）：同じ理由で`/bt-apply`のメール・郵便番号の形式チェックによる
   `400`エラーを削除。書式が変でも申込み自体は通す（メールが不正な形式のときは、受付完了メールの
   送信だけを内部でスキップする既存の分岐がそのまま効く）。
4. **「対象」欄に注記を追加**：「小学4年生以上」の下に「※小学4年生以下のお子さんの参加を希望される方は、
   ページ最後の「質問・ご要望」欄にご記入ください。」を追加（ロイさんの指示）。

### わかったこと・つまずいたこと
- 再現テストでは、必須項目のみ入力・任意項目を空欄にした状態で「確認する」→「送信する」まで
  問題なく進んだ（送信データも空文字列が正しく送られることをfetch横取りで確認）。ロイさんの
  環境固有の状況（ブラウザの自動入力が何か紛れ込んだ等）が疑われるが、特定はできなかったため、
  「そもそもチェックしない」形で対症療法的に解決した。

### 次にやること
- **未デプロイ。** `bt-kyoushitsu.html`はGitHub push（Cloudflare Pages自動デプロイ）、
  `material/worker.js`は`wrangler.cmd deploy`が必要。ロイさんの合図待ち。

### 動作確認
- ローカル静的サーバーで確認：
  - 何も入力せず「確認する」→赤太字・背景付きのエラーボックスで「「お名前」が入力されていません。
    ご記入をお願いします。」と表示されることを確認。
  - 必須項目のみ入力（任意項目は空欄）→エラー無く確認画面へ進むことを確認。
  - メールアドレス・郵便番号に不正な形式の文字列を入れても、エラー無く確認画面へ進むことを確認
    （fetchを横取りし、実際のメール送信・D1保存は発生させていない）。
  - 「対象」欄に注記が追加されていることを確認。
- ライブでの確認は未実施（未push・未デプロイ）。

---

## 2026-08-16（日）その32 — その31をデプロイ

### やったこと
- その31（住所欄の折り返し幅固定・行ごとの削除ボタン撤去）を本番に反映。

### 動作確認
- 本番HTMLに`row-del`が無いこと、`addr-cell`（住所欄の固定幅クラス）が
  3か所（style・th・td）に入っていることを確認。

## 2026-08-16（日）その31 — 住所欄の折り返し幅を固定・行ごとの削除ボタンを撤去（未デプロイ）

### やったこと
1. **住所欄を固定幅で折り返すように変更**（`bt-apply-admin.html`）。
   見出し幅に引っ張られていた原因は、`overflow-wrap:anywhere`が「どこでも改行してよい」
   という指定のため、table-layout:autoのアルゴリズムがこの列を「いくらでも縮められる列」と
   判断し、他のnowrap列に幅を譲って**指定した`width:14em`を無視して37.5pxまで潰していた**こと。
   `min-width:14em`を追加し、`overflow-wrap:anywhere`を`word-break:break-all`に変更して、
   最低でもその幅は確保されるようにした（実測で189px＝14em相当を確保、テーブル全体が
   その分だけ横スクロールするようになった）。
2. **行ごとの「削除」ボタンを撤去**（`bt-apply-admin.html`）。
   チェックボックス＋「選択した行を削除」で1件からでも削除できるため、行末の個別ボタンは
   冗長と判断し、列ごと削除した（`.row-del`関連のHTML・JS・CSSをすべて削除）。

### わかったこと・つまずいたこと
- table-layout:autoのテーブルで「ある列だけ幅を固定して折り返したい」場合、
  `width`（や`max-width`）だけでは、その列の中身が「どこでも改行できる」ほど、
  ブラウザが幅を勝手に縮めてしまうことがある。**`min-width`を併用して下限を守らせる**のが
  確実な対策。

### 動作確認
- ローカルサーバーで、住所欄の実測幅が189px（14em相当）に固定され、長い住所が複数行に
  折り返されることを確認（修正前は37.5pxまで潰れて1文字ずつ縦に伸びる状態だった）。
- 行ごとの削除ボタンが表示から消えていること、チェックボックス＋一括削除ボタンで
  引き続き削除できることを確認（コンソールエラー無し）。

## 2026-08-16（日）その30 — 「閉じる」ボタンの仕様確認・その29をデプロイ

### 確認したこと
「送信完了画面の閉じるボタンでタブごと閉じてほしい」と再度依頼があったが、
**その21ですでに`window.close()`を実装・デプロイずみ**だった。動かないように見えたのは、
ブラウザの仕様上、**スクリプトで開いたタブでない限り`window.close()`は効かない**ため
（URLを直接開いた場合はほぼ全員これに該当。Chrome・Edge・Safari共通のセキュリティ制限で、
Webページ側からは回避不可能）。
ロイさんに選んでもらい、**現状の実装（閉じようと試みて、閉じられなければ案内文に切り替える）
のまま維持**することで決着。コード変更は無し。

### やったこと
- その29（郵便番号・住所欄の`autocomplete`を`off`に変更）を本番にデプロイ。

### 動作確認
- 本番HTMLで`f-postal`の`autocomplete="off"`を確認。

## 2026-08-16（日）その29 — 郵便番号・住所欄の「入力例」の正体はブラウザの自動入力だった（未デプロイ）

### 起きていたこと
その27で`placeholder`（HTMLの入力例）はすべて消したはずなのに、ロイさんの環境では
郵便番号・住所の欄に何か「入力例」らしきものが入ってしまい、それが原因で
郵便番号→住所の自動入力が動かなくなっていた。

### 原因
`placeholder`ではなく、**ブラウザ自体の自動入力機能**だった。
この2つの欄には`autocomplete="postal-code"`・`autocomplete="address-line1"`を
指定していたため、ブラウザが「郵便番号／住所の欄です」と認識し、ブラウザに保存されている
（あるいはブラウザが持っている）住所情報を候補として出したり、勝手に入れてしまっていた。
このコードの`f-postal`のinputイベント処理は「住所欄が空のときだけ自動入力する」設計のため、
ブラウザが先に何か入れてしまうと、それだけでこちらの自動入力が動かなくなる仕組みだった。

### やったこと
- `bt-kyoushitsu.html`：郵便番号・住所の両欄の`autocomplete`を`off`に変更
  （`postal-code`・`address-line1`から`off`へ）。ブラウザ側の自動入力・候補表示を止めた。

### 動作確認
- ローカルサーバーで、ページを開いた直後の郵便番号・住所欄が空であること、
  `autocomplete="off"`になっていることを確認。
- `7670004`を入力して「香川県三豊市高瀬町比地」が正しく自動入力されることも再確認。

## 2026-08-16（日）その28 — その27をデプロイ

### やったこと
- その27（入力例の削除）を本番に反映。

### 動作確認
- 本番HTMLに`placeholder=`が1件も無いことを確認。

## 2026-08-16（日）その27 — 入力例をすべて削除／郵便番号の自動入力は正常だった（未デプロイ）

### やったこと
- `bt-kyoushitsu.html`の全入力欄から`placeholder`（入力例）を削除
  （お名前・フリガナ・電話番号・メール・郵便番号・住所・質問要望、7か所）。
- 郵便番号欄のヒント文言を「ハイフン（－）無しの7桁で入力すると〜」に変更（実際の使い方に合わせた）。

### 調査したこと：「7970004→香川県三豊市高瀬町比地」が出ない件
- 実際に`zipcloud`APIへ問い合わせたところ、**`797-0004`は愛媛県西予市宇和町伊崎**で、
  香川県三豊市高瀬町比地とは別の場所だった。**正しい郵便番号は`767-0004`**
  （`797`ではなく`767`）。ロイさんが例に挙げた番号が入れ違いだったと判明。
- `767-0004`で実際にテストしたところ、「香川県三豊市高瀬町比地」が正しく自動入力された。
  **自動入力の仕組み自体にバグは無かった。**

### 動作確認
- ローカルサーバーで、郵便番号欄に`7670004`（ハイフン無し7桁）を入力すると、
  住所欄に「香川県三豊市高瀬町比地」が正しく自動入力されることを実測で確認。
- 全入力欄からplaceholderが消えていることを確認。

## 2026-08-16（日）その26 — 住所→郵便番号の自動入力は、今回は見送り（ロイさんの裁定）

### 決まったこと
- 本来ロイさんがご希望だったのは「住所→郵便番号」の自動入力（その25でのこちらの
  解釈どおり）だったが、**精度の高い無料サービスが無いため、今回は実装しない**。
  現状の「郵便番号→住所」の自動入力のままでOK、とのこと。
- 将来、この逆方向が必要になった場合は、有料の住所検索APIを使うか、
  ある程度の精度低下を許容する形で別途相談する。

## 2026-08-16（日）その25 — 住所（任意）を追加、郵便番号から自動入力

### やったこと
1. **D1に列を追加**（本番）：`bt_applications`に`postal_code`・`address`（どちらもTEXT・任意）。
2. **`worker.js`を更新してデプロイ**。
   - `/bt-apply`が`postalCode`・`address`を受け取るようになった（どちらも任意）。
   - 郵便番号は、入力されていれば「000-0000」または「0000000」の形式チェックのみ行う
     （無くてもよい）。
   - 通知メール・受付完了メール・D1保存・管理者一覧（`/bt-applications`）のSELECT、
     すべてに反映。
3. **`bt-kyoushitsu.html`に「郵便番号」「住所」欄を追加**（メールアドレスの下、どちらも任意）。
   - **郵便番号を7桁入力すると、住所欄が空の場合だけ自動で住所を入力する**
     （無料の郵便番号API「zipcloud」を使用。CORS対応・APIキー不要）。
     すでに住所欄に何か書いてあるときは、自動入力で上書きしない。
   - 確認画面にも郵便番号・住所の行を追加（未入力なら「（未入力）」表示）。
4. **`bt-apply-admin.html`**：一覧テーブル・CSV書き出しの両方に「郵便番号」「住所」列を追加。

### わかったこと・つまずいたこと
- **「郵便番号は自動で出せるなら」の解釈について**：ご依頼の文面は「住所から郵便番号を
  自動で出す」とも読めたが、住所（自由記述のテキスト）から郵便番号を高い精度で逆引きできる
  無料サービスは実質無い。逆に「郵便番号→住所」は`zipcloud`という定番の無料APIがあり、
  信頼性も高いため、**今回はこちらの向き（郵便番号→住所）で実装した**。もし「住所を打ったら
  郵便番号が出る」方が良ければ、別途相談させてください（精度は保証しづらい）。
- デプロイ直後の1回目のテスト送信で、`postal_code`・`address`がD1に`null`で保存される
  現象が発生。Cloudflare Workersのデプロイ直後は、エッジの拠点によって新旧バージョンが
  数秒間混在することがあるためと判明（2回目のテストでは正しく保存された）。実害なし。

### 動作確認
- 本番`/bt-apply`にテスト送信し、`postal_code`・`address`がD1に正しく保存されることを確認
  （デプロイ直後の1回目は反映前だったため、少し待って再テストして確認。テストデータは削除ずみ）。
- ローカルで、郵便番号「767-0011」→住所「香川県三豊市高瀬町下勝間」の自動入力を実測で確認。
  すでに住所欄に手入力がある場合は上書きされないことも確認。
- 確認画面に郵便番号・住所の行が正しく表示されることを確認。
- 管理ページ（`bt-apply-admin.html`）で、コンソールエラー無しを確認
  （実データでの表示・CSV出力は、デプロイ後にあらためて確認）。

## 2026-08-16（日）その24 — 申込み一覧に削除機能・CSV書き出し機能を追加してデプロイ

### やったこと
1. **行ごとの削除・一括削除**（`bt-apply-admin.html` + `worker.js`）。
   - `worker.js`の`/bt-applications`にDELETEメソッドを追加。
     `?key=...&ids=1,2,3`のようにカンマ区切りで1件でも複数件でも削除できる。
   - 管理ページの一覧にチェックボックス列（各行＋ヘッダーの「すべて選択」）と、
     行ごとの「削除」ボタンを追加。選択件数を表示するバー（一括削除ボタン付き）を新設。
     どちらも実行前に確認ダイアログを出す（元に戻せないため）。
2. **Excel・スプレッドシート用のCSV書き出し**（今回の本題）。
   - 一覧を読み込んだ時点のデータを`lastRows`に保持しておき、「Excel用ファイルを保存（CSV）」
     ボタンでCSVに変換して書き出す。カンマ・改行・ダブルクォートを含む項目は
     正しくエスケープ（`""`で囲む）。
   - **文字化け対策として、ファイルの先頭にUTF-8のBOM（`EF BB BF`）を付けている**
     （無いとExcelで日本語が文字化けする）。
   - **保存先を選べるようにする指定への対応**：`showSaveFilePicker`（File System Access API）に
     対応しているブラウザ（Chrome・Edgeなど）では、保存先を選べるダイアログを出す。
     対応していないブラウザ（Safari・Firefoxなど）は、ふつうのダウンロード
     （既定のダウンロードフォルダへの保存）にフォールバックする。
   - ファイル名は「バウンドテニス教室_申込み一覧_YYYYMMDD.csv」を自動で提案。

### わかったこと・つまずいたこと
- 本番確認で`curl`が308リダイレクト（`.html`付き→無しのURLへ）を返しているのを見落とし、
  `-L`（リダイレクトを追う）を付け忘れて「反映されていない」と一瞬勘違いした。
  `bt-apply-admin.html`のような`.html`付きURLを直接`curl`するときは、必ず`-L`を付けること。
- CSVのBOMは、`Blob.text()`で読み返すと消えて見える（`TextDecoder`が既定でBOMを取り除く仕様の
  ため）。実際にファイルへ書き出したときの生バイト列（`EF BB BF`）で確認する必要がある。
  今回は`arrayBuffer()`で先頭バイトを見て確認した。
- 本番のD1に、**ロイさんが実際にテストで使ったとみられる申込みデータ（id 4〜6）**が
  すでに入っていた。削除機能のテストでは、自分が作ったテスト行（id 7）だけを対象にし、
  それ以外には触れていない。

### 動作確認
- Workerを先にデプロイし、本番の`/bt-applications`にテスト用の申込みを1件作成→
  DELETEエンドポイントで削除→D1から消えていることを実測で確認（他のデータは無事）。
  間違った管理キーでは削除できないことも確認。
- CSV書き出しは、`fetch`をモックしたローカル環境で、ヘッダー行・エスケープ処理・BOMの
  有無（生バイトで確認）・保存先を選べないブラウザ向けのフォールバック動作を確認。
- フロントエンド（`bt-apply-admin.html`）をデプロイし、本番で削除ボタン・CSV書き出しボタンが
  配信されていることを確認（`curl -L`で確認）。

## 2026-08-16（日）その23 — その22をデプロイ（今回はindex.htmlも含めて公開）

### やったこと
- その22（正式版ポスター・開催概要・「仮」表記の撤去）を本番に反映。
- **今回はトップページ（`index.html`のお知らせ欄）も含めて公開した**
  （これまでは`git stash`で毎回除外していたが、ポスターが正式版になったので
  お知らせ欄ごと表に出す判断。ロイさんに確認済み）。

### 動作確認
- 本番トップページに`id="notice"`のお知らせ欄が表示されていること、
  「仮のもの」の文言が本番のどちらのページにも残っていないこと、
  申込みフォームに「吉津小学校」など開催概要が表示されていることを確認。

## 2026-08-16（日）その22 — 正式版ポスターに差し替え・止まっていた作業を消化（未デプロイ）

ロイさんの指示：「ポスターが更新されたので差し替え。それに伴い止まっていた作業もお願いします。
ただしドメイン関係はまだ」。

### やったこと
1. **正式版ポスターに差し替え**。
   `Desktop\RoyWorkingFolder\02-資料・レポート\R8年度三豊市スポーツ協会関係\
   バウンドテニス教室チラシ-20260816-01.JPG`（8000×4500、白い余白付き）を、
   実際のポスター部分だけ切り出し（3138×4470）→ 幅900pxに縮小・軽量化（約257KB）→
   これまでと同じファイル名 `image/bt-kyoushitsu-flyer-kari.jpg` に上書き保存。
   （ファイル名は変えていないが、中身は正式版。参照している`bt-kyoushitsu.html`・
   `index.html`とも、コード変更なしでそのまま新しい絵に切り替わる）。
2. **開催日の食い違いが解消ずみだったことを確認**。新しいポスターの日程
   （10/10・10/17・10/24・11/7、いずれも土曜）は、既存のフォーム・`worker.js`の
   `BT_DATES`と**完全に一致**していた。日程まわりのコード変更は不要だった。
3. **「仮」の表記をすべて撤去**。
   - `bt-kyoushitsu.html`：ポスターの「案内ポスター（仮）」バッジ・「正式版が完成し次第、
     差し替えます」の注記を削除。
   - `index.html`のお知らせ欄も同様に「仮のもの」の注記を削除。
   - 両ファイルの`alt`属性からも「（仮）」を外した。
4. **止まっていた「会場・時間・持ち物」の掲載を実施**（これまで仕様書に無く未掲載だった分。
   正式ポスターで判明したので追加）。
   - `bt-kyoushitsu.html`：ポスターの下に「開催概要」の情報ブロック（`.details`）を新設。
     時間（午後7:30〜9:00）・場所（吉津小学校）・対象（小学4年生以上）・持ち物
     （体育館シューズ、タオル、飲み物／ラケット貸し出しあり）・受付締切（10月2日(金)）。
   - `index.html`のお知らせ欄にも、同じ内容を1行の要約（場所・対象・受付締切）で追加。
   - **対象年齢（小学4年生以上）の入力チェックは追加していない**（生年月日は集めているが、
     学年の判定ロジックは複雑になるため今回は見送り。情報として掲示するのみ）。
   - **受付締切（10月2日）による送信の締め切り処理も追加していない**（表示のみ。
     締切後も技術的には送信できる状態。締切管理が必要なら別途対応）。

### わかったこと・つまずいたこと
- 新しいポスター画像は横長のJPG（8000×4500）の中に、ポスター本体（縦長）が
  白い余白に囲まれて入っていた。PythonでRGB値が白でない範囲を検出して自動で
  範囲を割り出し、そこだけ切り出した（`col range 2431-5568`、行方向はほぼ全面）。
- **ドメイン関係（Resend独自ドメイン・CC・受付完了メール）は、指示どおり今回は触っていない。**

### 次にやること
- デプロイ待ち（`index.html`を含め、今回はすべて公開してよいか確認してから）。
- 独自ドメイン取得後、CC・受付完了メールを有効化する（別作業として保留のまま）。

### 動作確認
- ローカルサーバーで、`bt-kyoushitsu.html`に開催概要ブロックが正しい内容で表示されること、
  `index.html`のお知らせ欄も新しい内容に更新されていること、「仮」の表記がどちらの
  ページにも残っていないこと、横のはみ出しが無いことを確認。

## 2026-08-16（日）その21 — 「閉じる」ボタンでタブを閉じる＋その20をまとめてデプロイ

### やったこと
- `bt-kyoushitsu.html`：送信完了画面の「閉じる」ボタンを、`window.close()`で
  **タブそのものを閉じる**動作に変更。
  - ブラウザの仕組み上、スクリプトから開いたタブでない場合は閉じられないことがあるため、
    400ms待っても閉じられなかった場合にそなえて「このタブを閉じてお進みください。」という
    案内文に切り替えるフォールバックを入れた（見出しだけ残る中途半端な画面にはならない）。
- その20（管理ページの自動表示をやめたセキュリティ修正）と合わせて本番にデプロイ。
  いつも通り`index.html`だけ`git stash`で除外→デプロイ後に復元ずみ。

### わかったこと・つまずいたこと
- ローカル確認中、`window.close()`で実際にタブが閉じてしまい、フォールバック文言の
  見た目までは確認できなかった（閉じる処理自体が動くことは確認できた）。
  実際のブラウザ・開き方によっては閉じられないケースもありうるので、その場合は
  フォールバック文言が出る設計にしている。

### 動作確認
- ローカルサーバーで「閉じる」ボタン押下→タブが実際に閉じることを確認。
- 本番HTMLで、管理ページに自動読み込みコード（`if (savedKey) load()`）が
  含まれていないこと、申込みフォームに`window.close`の処理が入っていることを確認。
- トップページ（`index.html`）は今回も変更されていないことを確認。

## 2026-08-16（日）その20 — 管理ページの自動表示をやめた（セキュリティ修正・未デプロイ）

### 指摘されたこと
管理ページを開くと、以前入力した管理キーがブラウザに保存されていて、**キーを入れる操作なしに
申込み一覧が全件表示されてしまっていた**。ページのURLさえ知っていれば、キーを知らない人にも
中身が見える状態だった（ヘッダーに「管理用」リンクを置いたばかりだったので、これは直さないと
まずい状態）。

### やったこと
- `bt-apply-admin.html`：管理キーを**ブラウザに保存しない**ように変更した。
  - ページを開いたときの自動読み込み（`if (savedKey) load();`）を削除。
  - `localStorage`への保存（`localStorage.setItem('bt_admin_key', ...)`）を、
    一覧読み込み成功時・管理キー変更成功時の両方から削除。
  - **ページを開いた瞬間に、以前のバージョンで保存されていた分を消す処理**を追加
    （`localStorage.removeItem('bt_admin_key')`）。これにより、この修正を配る前に
    誰かのブラウザに残っていた古い保存分も、次に開いた時点で消える。
  - 「管理キーを変更する」欄のヒント文言も、保存しなくなったことに合わせて書き直した。
- これで、**管理キーを毎回正しく入力して「表示する」を押さない限り、何も表示されない**
  （申込み一覧・CC宛先一覧・送信元アドレスの現在値、すべて共通）。

### わかったこと・つまずいたこと
- 「記号が複雑すぎて覚えられない」への対応として、その7で管理キーの自己変更機能と
  ブラウザへの保存機能を作ったが、**保存機能そのものが今回の情報露出の原因**になっていた。
  利便性とセキュリティが直接ぶつかる形になったため、今回は**保存しない方を優先**した
  （ロイさんがすでに覚えやすいキーに変更ずみなので、毎回入力する負担は小さいはずという判断）。

### 次にやること
- 特になし（デプロイ待ち）。

### 動作確認
- ローカルサーバーで、`localStorage`にわざと古いキーを仕込んだ状態でページを開き、
  - キー入力欄が空であること
  - `localStorage`のキーが消えていること
  - 申込み一覧（`#tablewrap`）が非表示のままであること
  を実測で確認。

## 2026-08-16（日）その19 — その18（見出し下テキスト削除）をデプロイ

### やったこと
- その18の変更（「はじめての方こそ〜」削除・余白調整）を本番に反映。
  いつも通り`index.html`だけ`git stash`で除外してデプロイ→デプロイ後に復元ずみ。

### わかったこと・つまずいたこと
- 確認の`grep`で最初「はじめての方こそ」が本番に残っているように見えたが、これは
  `<meta property="og:description">`タグ内の別の似た文言（「はじめての方こそ大歓迎。」）に
  ヒットしていただけだった。表示テキスト本体（`class="sub"`の段落）は正しく消えている
  ことを`class="sub"`個別検索で確認した。og:descriptionは元から別の文なので変更不要。

### 動作確認
- 本番HTMLに`class="sub"`が存在しないことを確認。
- トップページ（`index.html`）は今回も変更されていないことを確認。

## 2026-08-16（日）その18 — 見出し下の「はじめての方こそ〜」を削除・余白を調整（未デプロイ）

### やったこと
- `bt-kyoushitsu.html`：見出し（h1）の下にあった「はじめての方こそ、大歓迎です。」
  （`.sub`）を削除。
- 見出しから挨拶文＋ポスターの枠（`.intro-card`）までの間隔を詰めた
  （`.hero h1`の下マージンを4px→0、`.intro-card`の上マージンを26px→20px）。

### 動作確認
- ローカルサーバーで、`.sub`要素が無いこと、h1の下端から`.intro-card`上端までが
  20pxになっていることを実測で確認。

## 2026-08-16（日）その17 — 「閉じる」ボタン・完了画面の絵をデプロイ

### やったこと
- その15（閉じるボタン）・その16（完了画面の絵の差し替え・透過画像）を本番に反映。
  いつも通り`index.html`（お知らせ欄）だけは`git stash`で一時的に除外してデプロイ
  （デプロイ後に`git stash pop`で復元ずみ）。
- Workerの変更は無し（今回はフロントエンドのみ）。

### 動作確認
- 本番URLで`image/bt-good-work.png`が200で取得できること、`bt-kyoushitsu`のHTML内に
  同ファイル名の参照が入っていることを確認。
- トップページ（`index.html`）は今回も変更されていないことを確認。

## 2026-08-16（日）その16 — 送信完了画面の絵を差し替え（未デプロイ）

### やったこと
- 送信完了画面（🎾の絵文字だった部分）を、ロイさんが用意した画像
  （`Pictures\新しいフォルダー (3)\good work.png`／テニス少年少女の「おつかれさまぁ♪」イラスト）
  に差し替えた。
- **背景を透過に加工**してから設置。手順：
  1. Pythonで背景色（クリーム色、四隅の実測値からRGB約(255,236,195)）を基準に、
     色の近さに応じてなだらかにアルファ値を落とす処理（境界のギザギザ防止のためのフェザー処理）。
  2. 透過後の余白をトリミング（bboxで自動検出）。
  3. 表示サイズに合わせて360px幅に縮小し、ファイルサイズを軽量化（约750KB→约220KB）。
  4. `image/bt-good-work.png`として保存。
- `bt-kyoushitsu.html`：`.done .em`を絵文字のdivから`<img>`タグに変更し、
  幅180px（最大60%）・中央寄せのスタイルに調整。

### 動作確認
- ローカルサーバーで、透過処理後の画像をクリーム色背景に合成したプレビュー画像を目視確認
  （背景の色ムラ無く綺麗に抜けていることを確認。人物の輪郭線・影の楕円はそのまま残っている）。
- ページ上でも画像が正しく読み込まれ、中央に約160px幅で表示されることを実測で確認。

## 2026-08-16（日）その15 — 送信完了画面のボタンを「閉じる」に変更（未デプロイ）

### やったこと
- `bt-kyoushitsu.html`の送信完了画面（`#f-done`）のボタンを、
  「Roy's Channel のトップへ」（リンク）→「閉じる」（ボタン）に変更した。
  押すと完了メッセージが非表示になる（ページ自体はそのまま。トップへ行きたい場合は
  フッターの既存リンクを使う想定——ロイさんの判断）。

### 動作確認
- ローカルサーバーでJS実行により、「閉じる」ボタンが`<button>`要素であること、
  押すと`#f-done`が`display:none`になることを確認。

## 2026-08-16（日）その14 — CC・受付完了メールは、独自ドメイン取得まで保留（ロイさんの裁定）

### 決まったこと
- **今回は独自ドメインを取得していないので、CC機能・申込者への受付完了メールは
  使えなくてOK**（ロイさんの判断）。申込み受付そのものは正常に機能しているので、
  今はこれで問題ない。
- **独自ドメインを取得したら、これらの機能を有効化する**。作業自体はコード側で
  すでに実装ずみなので、そのときやることは以下の2つだけ：
  1. [resend.com/domains](https://resend.com/domains)で独自ドメインを認証
  2. 管理ページ（`bt-apply-admin.html`）の「申込みメールの送信元アドレス」欄で、
     そのドメインのアドレスに変更する
  これだけで、CC・受付完了メールの両方が実際に届くようになる（コードの追加変更は不要）。

### 次にやること
- 独自ドメイン取得・Resend認証が済んだら、上記の送信元アドレス変更を行う。
  それまでは現状維持でよい。

## 2026-08-16（日）その13 — 「確認画面で送信失敗」の原因究明・修正・デプロイ

### 起きていたこと
確認画面で「この内容で送信する」を押すと、本番で毎回「送信に失敗しました」になっていた。

### 原因
バグではなく、**Resendの「お試し用」送信元アドレス（onboarding@resend.dev）の制限**。
このアドレスは「アカウント本人のメールアドレス（roy.s1105@gmail.com）以外には一切送れない」
という決まりになっている。ロイさんが管理ページのCC機能を実際に試し、ご自身の別のアドレス
（yahoo.co.jp）をCCに登録していたため、申込みメール送信そのものがResend側で拒否されていた
（`wrangler tail`でエラー本文を確認：`403 validation_error / You can only send testing
emails to your own email address`）。

### やったこと
1. **CCが原因で送信全体が止まらないようにした**（`worker.js`の`handleBtApply`）。
   CC付きで送信 → 失敗したらCC無しでもう一度だけ送り直す、というフォールバックを追加。
   これで、管理者側のCC設定に問題があっても、**申込み自体は必ず通る**ようになった
   （CCには届かなくなるだけ）。
2. **申込みメールの送信元アドレスを、管理ページから変更できるようにした**（今回の本質的な解決策）。
   - 新しいD1テーブル`bt_mail_from`（1行だけ）。無ければ今まで通り`onboarding@resend.dev`。
   - 新エンドポイント`GET/POST /bt-mail-from`（現在値の取得・変更、要管理キー）。
   - `bt-apply-admin.html`に「申込みメールの送信元アドレス」欄を追加。現在の送信元を表示し、
     変更できるようにした。**Resendで独自ドメインを認証してから、そのドメインのアドレスに
     変更しないと、依然としてCC等はロイさん以外の宛先に届かない**旨を、赤字の注意書きで明記。
3. D1に`bt_mail_from`テーブルを作成（本番）。
4. Workerをデプロイ（`dev/tool/senior-site-api`で`wrangler.cmd deploy`）。
5. **フォーム・管理ページだけを本番に反映**（前回同様、`index.html`は`git stash`で
   一時的に前回コミット時点へ戻してからデプロイ→デプロイ後に`git stash pop`で復元）。

### わかったこと・つまずいたこと
- 本番のエラー原因が分からないときは、`wrangler.cmd tail --format pretty`で実際のログを
  見るのが早い。`console.error`で出していたResendのエラー本文がそのまま見えた。
- **ロイさんはすでにご自身で管理キーを新しいもの（メモしやすい方）に変更ずみ**、
  CC宛先も1件（ご自身のyahoo.co.jp）登録ずみだった。今回の修正により、この状態のままでも
  申込み自体は問題なく通る（CCだけ届かない状態）。独自ドメインを認証すれば、CCも届くようになる。

### 次にやること
- Resendで独自ドメインを認証し、管理ページの「送信元アドレス」をそのドメインのものに変更すると、
  CC機能・受付完了メール（申込者ご本人あて）の両方が実際に届くようになる。
  （受付完了メールも、今の`onboarding@resend.dev`のままだと、申込者がロイさん以外の場合は
  同じ理由で届かない。これは失敗してもエラーにはならず、静かに送られないだけ。）

### 動作確認
- 本番で`/bt-apply`を実測：CC宛先が登録された状態でも`{"ok":true}`になることを確認
  （テストデータは確認後に削除）。
- `/bt-mail-from`（GET）を本番で実測：現在の送信元アドレスが取得できることを確認。
- 本番URLで`bt-kyoushitsu`・`bt-apply-admin`とも最新版が配信されていること、
  トップページ（`index.html`）は変更されていないことを確認。

---

## 2026-08-16（日）その12 — 確認画面では挨拶文・案内ポスターを隠す（未デプロイ）

### やったこと
- `bt-kyoushitsu.html`：確認画面（`#f-confirm`）を表示している間は、
  挨拶文＋案内ポスターの枠（`#intro-card`）も一緒に隠すようにした。
  確認画面はすでに入力ずみの内容を見るためのものなので、案内文やポスターは不要という判断。
  「戻る」を押すと、入力画面と一緒に元通り表示される。

### 動作確認
- ローカルサーバーでJS実行により、確認画面表示時に`#intro-card`が`display:none`に
  なること、「戻る」を押すと`display:block`に戻ることを確認。

## 2026-08-16（日）その11 — 申込みフォーム・管理ページだけをデプロイ（index.htmlは据え置き）

ロイさんの指示：「申込みフォームと管理用のみデプロイ。Roy's Channelメインページ（トップページの
お知らせ欄）はまだデプロイしない」。

### やったこと
1. **D1のスキーマ変更を本番に適用**（その4・その7で書いていた分すべて）。
   - `bt_applications`に`birthdate`列を追加
   - `bt_cc_recipients`テーブルを新規作成（CC宛先の管理）
   - `bt_admin_key`テーブルを新規作成（管理キーの自己変更用）
2. **Workerをデプロイ**（`dev/tool/senior-site-api`で`wrangler.cmd deploy`）。
   `/bt-apply`（生年月日→年齢計算・メール任意・CC自動付与）、`/bt-cc-recipients`
   （一覧・追加・削除）、`/bt-admin-key`（管理キー変更）を本番で実際にテストして確認：
   - CC宛先の追加→一覧に反映→削除→一覧から消える、を実測。
   - 管理キーを一時的に別の値に変更→新しいキーで認証できる／古いキーで拒否されることを確認
     →**テスト後、元のキーに戻した**（本番の管理キーは変更前のまま）。
3. **`index.html`（トップページのお知らせ欄）だけを含めずにデプロイ**。
   `wrangler pages deploy .`はフォルダ全体を1回のデプロイでアップロードする仕組みなので、
   「このファイルだけ除外する」という機能が無い。そこで、
   - `git stash push -- index.html`で、ロイさんが編集中の`index.html`（お知らせ欄入り）を
     一時的に前回コミット時点（お知らせ欄なし＝今までの本番と同じ内容）に戻す
   - その状態で`wrangler.cmd pages deploy .`を実行（`bt-kyoushitsu.html`・`bt-apply-admin.html`・
     ポスター画像はロイさんが最新編集した内容のまま、`index.html`だけ元の内容でアップロードされる）
   - デプロイ完了後、`git stash pop`でロイさんの`index.html`編集を即座に復元（ローカルの
     お知らせ欄はそのまま残っている。単に**今回のデプロイに含めなかった**だけ）。

### わかったこと・つまずいたこと
- Cloudflare Pagesは、デプロイ直後は`roys-channel.pages.dev`（本番エイリアス）側で
  エッジキャッシュが少し残ることがある（クエリ文字列でキャッシュを避けると最新が見える）。
  今回も`/bt-cc-recipients`の初回確認で一瞬古い応答が出たが、キャッシュ避けで解決した。
  実害はない（数分で自然に反映される）。
- `wrangler pages deploy .`は「そのフォルダの中身を丸ごと1回で公開する」仕組みなので、
  一部のファイルだけを選んでデプロイすることはできない。複数ファイルが同時に編集中で、
  一部だけ先に出したいときは、今回のように`git stash`で該当ファイルだけ一時的に
  「前回の状態」に戻してからデプロイする、という手順を使う。

### 次にやること
- `index.html`のお知らせ欄（お知らせ欄・案内ポスター）を公開するタイミングになったら、
  あらためて`wrangler.cmd pages deploy .`を実行する（今度は`index.html`も含めて）。

### 動作確認
- 本番URL（`https://roys-channel.pages.dev/bt-kyoushitsu`・
  `https://roys-channel.pages.dev/bt-apply-admin`）で、タイトルタグを実測して
  最新版が配信されていることを確認。
- 本番トップページ（`https://roys-channel.pages.dev/`）に`id="notice"`の
  お知らせセクションが**含まれていないこと**を確認（今回デプロイしていないので当然だが、
  念のため実測で確認）。

---

## 2026-08-16（日）その10 — ヒーローの余白調整＋管理画面から戻るリンクを追加（未デプロイ）

### やったこと
- `bt-kyoushitsu.html`：「運営・管理は～」の一文まわりの余白を調整。
  - 上の余白：`.hero`の上パディングを44px→24pxに縮めた。
  - バッジ（BOUND TENNIS）との間：`.operator`の下マージンを12px→20pxに広げた。
- `bt-apply-admin.html`の先頭に「← 申込みフォームへ戻る」リンクを追加
  （`bt-kyoushitsu.html`へ）。管理画面から申込みフォームへ、フォームのヘッダーから
  管理画面へ、双方向に行き来できるようになった。

### 動作確認
- ローカルサーバーで、余白の実測値（上24px・バッジ手前20px）とリンクの表示・リンク先を確認。

## 2026-08-16（日）その9 — ヘッダーに管理画面へのリンクを追加（未デプロイ）

### やったこと
- `bt-kyoushitsu.html`のヘッダーに「管理用」リンクを追加（`bt-apply-admin.html`へ）。
  「トップへ」より控えめな見た目（小さめの文字・グレー）にして、目立たせすぎないようにした。
  設置場所は「申込みフォームのページのヘッダー」をロイさんに選んでもらった
  （トップページ`index.html`には入れていない＝サイト全体の訪問者には見えにくい）。

### 動作確認
- ローカルサーバーで、PC幅・スマホ幅（375px）とも「管理用」「トップへ」がヘッダー右端に
  正しく並び、横のはみ出しが無いことを確認。リンク先(`bt-apply-admin.html`)も確認。

---

## 2026-08-16（日）その8 — 運営者表記を追加（未デプロイ）

### やったこと
- `bt-kyoushitsu.html`の見出し上、「BOUND TENNIS」のバッジの上に
  「運営・管理は【Roy's Channel】がおこなっています」の一文をさりげなく追加
  （小さめの文字・控えめな色。目立たせすぎない`.operator`クラス）。

### 動作確認
- ローカルサーバーで表示を確認。

---

## 2026-08-16（日）その7 — CC追加エラーの原因判明・管理キーを自分で変更できるように（未デプロイ）

### やったこと
- **管理ページでCC宛先の「追加」を押すとエラーになる件を確認**。原因はバグではなく、
  **まだ本番にデプロイしていない**ため。本番の`roy-senior-site-api`にはまだ
  `/bt-cc-recipients`エンドポイントも`bt_cc_recipients`テーブルも存在しないので、
  管理ページから叩くと404になり「追加に失敗しました」と出る。
  - 追加後に一覧へ新しい行が増える動き・削除ボタンは、**その4のセッションで実装ずみ**
    （`addCc()`が成功後に`loadCcList()`で一覧を再読込、`removeCc()`に確認ダイアログ付きの
    削除ボタン）。デプロイすれば動くはずのものなので、コード側の追加対応は不要だった。
- **管理キーを、ロイさん自身が管理ページから変更できるようにした**（`wrangler secret put`を
  使わなくても済むように）。
  - `worker.js`：管理キーの判定を`env.ADMIN_KEY`直接比較から、`getEffectiveAdminKey()`経由に変更。
    D1の`bt_admin_key`テーブル（1行だけ）に値があればそちらを優先、無ければ今まで通り
    `wrangler secret put`で設定した`ADMIN_KEY`を使う——という優先順位にした
    （「最初は今のまま、その後は自分で登録」の指示どおり）。
  - 新エンドポイント`POST /bt-admin-key`（`{currentKey, newKey}`）：現在のキーが合っていれば、
    新しいキーをD1に保存する。以後の認証はすべて新しいキーで行う。
  - `bt-apply-admin.html`に「管理キーを変更する」の折りたたみ欄を追加
    （新しいキーを2回入力→一致確認→変更。成功したら、キー入力欄とこのブラウザの保存も
    自動で新しいキーに更新される）。

### わかったこと・つまずいたこと
- CCの「追加でエラー」は、コードの不具合ではなく**未デプロイが原因**だった。
  今後、本番でしか動かない機能をテストしてもらうときは、先に「まだデプロイしていません」と
  伝えるべきだった（今回はデプロイしないでの指示が続いていたため、後回しにしていた）。

### 次にやること（デプロイ時、この順番で）
1. D1のスキーマ変更（その4に書いたもの＋今回追加ぶん）。

   ```sql
   ALTER TABLE bt_applications ADD COLUMN birthdate TEXT;

   CREATE TABLE IF NOT EXISTS bt_cc_recipients (
     id INTEGER PRIMARY KEY AUTOINCREMENT,
     email TEXT NOT NULL UNIQUE,
     created_at TEXT NOT NULL
   );

   CREATE TABLE IF NOT EXISTS bt_admin_key (
     id INTEGER PRIMARY KEY CHECK (id = 1),
     key_value TEXT NOT NULL,
     updated_at TEXT NOT NULL
   );
   ```
2. `dev/tool/senior-site-api` で `wrangler.cmd deploy` → 本番のWorkerを更新。
3. `roys-channel` を公開（`wrangler.cmd pages deploy .` または GitHub push）。
4. 本番でCC追加・削除・管理キーの変更を実際にテストする。

### 動作確認
- ローカルサーバー（`bt-apply-admin.html`）で、「管理キーを変更する」欄・CC追加欄とも
  画面表示・コンソールエラー無しを確認（実際の変更・追加はバックエンド未デプロイのため未確認）。

---

## 2026-08-16（日）その6 — 挨拶文と案内ポスターをひとつの枠にまとめる（未デプロイ）

### やったこと
- `bt-kyoushitsu.html`で別々のカードだった「挨拶文（`.welcome`）」と「案内ポスター（`.poster`）」を、
  ひとつの白い枠（`.intro-card`）の中にまとめた。枠の背景・境界線・影は`.intro-card`だけが持ち、
  中の`.welcome`・`.poster`は素の状態にした（二重に枠が付かないように）。
- スマホ幅でも枠からのはみ出しが無いことを確認（375px幅で`document.body.scrollWidth`が
  ちょうど375pxに収まることを実測）。

### 動作確認
- ローカルサーバーで、PC幅・スマホ幅（375px）の両方を確認。
  - PC幅：`.intro-card`が挨拶文とポスター画像の両方を1つの白い枠（角丸22px・影付き）で
    囲っていることを、DOMの背景色・枠線の実測値で確認（挨拶文・ポスター側は透明・枠線0）。
  - スマホ幅：横のはみ出しゼロ、「案内ポスター（仮）」の吹き出しラベルも枠内に収まることを確認。

---

## 2026-08-16（日）その5 — 申込みフォームの「開催日」欄も案内ポスターに変更（未デプロイ）

### やったこと
- `bt-kyoushitsu.html` の冒頭にあった「開催日」のリスト表示（`.days`）を、トップページの
  お知らせ欄と同じ案内ポスター画像（`image/bt-kyoushitsu-flyer-kari.jpg`）に差し替えた。
  「案内ポスター（仮）」の吹き出しラベルと、「正式版が完成し次第、差し替えます」の注記付き。
  ロイさんの指示で**テスト中の仮表示**として設置。正式版のポスターができたら画像を差し替える。
- `.days`関連のCSS（ピル型リスト）は使わなくなったため削除し、`.poster`という新しいクラスに
  置き換えた（トップページの`.nt-poster`と考え方は同じ）。

### 次にやること
- その4に書いた内容（D1スキーマ変更・デプロイ）と合わせて、正式版ポスターができたら
  `image/bt-kyoushitsu-flyer-kari.jpg` を差し替える（ファイル名はそのままで上書きすればOK。
  `index.html`のお知らせ欄も同じファイルを参照しているので、1回の差し替えで両方に反映される）。

### 動作確認
- ローカルサーバー（`http://localhost:8892/roys-channel/bt-kyoushitsu.html`）で、
  開催日リストの位置に案内ポスター画像が表示されることを確認。

---

## 2026-08-16（日）その4 — 申込みフォームを大幅改修（未デプロイ・D1未移行）

ロイさんからのフィードバックを反映。**まだデプロイもD1のスキーマ変更も実行していない**
（前ターンで「デプロイはしないでください」の指示があったため、ローカルの編集のみ）。

### やったこと
1. **開催日の表示デザインを変更**（`bt-kyoushitsu.html`）。
   ボタンと紛らわしいという指摘のため、白いピル型カードをやめて、点線区切り＋📅アイコンの
   ただのリスト表示に変更（`.days li`）。押せそうに見えないデザインにした。
2. **「年齢（年代）」→「生年月日」に変更**（スポーツ保険の加入に必要なため）。
   - フォーム：`<select>` の年代選択をやめ、`<input type="date">` に変更。未来日は選べないよう
     `max` 属性をJSで当日にセット。
   - 送信時：**サーバー側（`worker.js`）で申込み時点の満年齢を計算して保存**する設計にした
     （`calcAgeJST()`。日本時間の「今日」基準）。フロント側でも参考表示用に同じロジックで
     年齢を計算し、確認画面に「（参考：満◯歳）」と出すが、**正式な記録はサーバー側の計算**。
   - D1の`bt_applications`に`birthdate`列を追加する必要がある（下記「次にやること」参照）。
3. **電話番号は必須のまま**。ハイフン無しでも送れる仕様は元から変わっていない
   （ヒントに「ハイフン（－）は無くても構いません」と明記しただけ）。
4. **メールアドレスを任意に変更**。未入力でも送信できる。受付完了メールは
   入力されている場合だけ送る（この動作はもともとそう作っていた）。
   メール未入力時は通知メール本文の「返信すると本人に届く」の一文を出さないようにした
   （`reply_to`が無いため）。
5. **CC機能の位置づけを変更**。申込者が入力するCC欄は廃止し、
   **管理者（ロイさん）が管理ページから登録する固定のCC宛先**に変更した。
   - 新しいD1テーブル `bt_cc_recipients`（メールアドレスの一覧・件数上限なし）が必要
     （下記「次にやること」参照）。
   - `worker.js` に新しいエンドポイント `/bt-cc-recipients` を追加
     （GET=一覧、POST=追加、DELETE=削除。すべて要ADMIN_KEY）。
   - `bt-apply-admin.html` にCC宛先の追加・削除UIを追加した（申込み一覧の上に新設）。
   - 申込み受付時、この登録済みCCリストを毎回読み込んで通知メールのCCに設定する
     （`handleBtApply`内）。
6. **送信前に確認画面を追加**（`bt-kyoushitsu.html`）。
   「この内容を確認する」ボタンで入力内容を検証したあと、送信はせずに確認画面
   （`#f-confirm`）を表示。内容一覧＋「← 戻って直す」「この内容で送信する」の2ボタン。
   「戻る」は入力画面に戻るだけで、入力値はそのまま保持される（DOMを隠しているだけなので）。
   「送信する」を押した時点で、はじめて`/bt-apply`にPOSTする。

### わかったこと・つまずいたこと
- **D1のスキーマ変更は「デプロイ」の一部として扱い、指示が来るまで実行していない。**
  今回はコード編集のみ。実際に公開するときは、以下のSQLを先に流してから
  `wrangler.cmd deploy`する必要がある（順番を間違えると、新しいコードがまだ無い列・
  テーブルを読み書きしようとしてエラーになる）。

  ```sql
  ALTER TABLE bt_applications ADD COLUMN birthdate TEXT;

  CREATE TABLE IF NOT EXISTS bt_cc_recipients (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    email TEXT NOT NULL UNIQUE,
    created_at TEXT NOT NULL
  );
  ```
  - 既存の`age`列（TEXT）はそのまま使い、中身を「60代」のような文字列から
    「41」のような計算済みの年齢に変える（列そのものは変更不要）。
  - 既存の`cc`列（申込者が入力した旧CC機能の名残）は、今後書き込まなくなるが、
    列自体は残しておいて問題ない（使われなくなるだけ）。消したければ別途どうぞ。
- 生年月日から年齢を計算する処理は、フロント（確認画面用・参考表示）とサーバー
  （正式な記録用）の2箇所に同じロジックを持たせた。ズレる可能性があるのは
  「確認画面を表示した日」と「送信ボタンを押した日」が違う場合（日をまたいだ場合）だが、
  正式な記録は送信時にサーバーが計算し直すので、実害はない。

### 次にやること
1. **D1のスキーマ変更を実行**（上記SQL）。
2. `dev/tool/senior-site-api` で `wrangler.cmd deploy` → 本番のWorkerを更新。
3. `roys-channel` を公開（`wrangler.cmd pages deploy .` または GitHub push）。
4. 本番で一通りテスト送信（メールあり／メールなし、CCありなしそれぞれ）。
5. 管理ページ（`bt-apply-admin.html`）からCC宛先を1件登録し、テスト申込みでCCに
   届くことを確認する。

### 動作確認
- ローカルサーバー（`http://localhost:8892/roys-channel/bt-kyoushitsu.html`）で、
  入力→確認画面表示（生年月日から年齢「満41歳」を正しく計算）→戻るボタンで入力値が
  保持されたまま入力画面に戻ることを確認。
  メールアドレス未入力のまま確認画面まで進めることも確認（「（未入力）」と表示）。
- **実際の送信（`/bt-apply`へのPOST）は行っていない**（バックエンドがまだ新しいD1列・
  テーブルに対応していない状態でデプロイもしていないため。デプロイ後に本番で確認する）。
- 管理ページのCC管理UI・一覧表示の見た目はコード上の確認のみ（実データでの動作は
  デプロイ後）。

---

## 2026-08-16（日）その3 — トップページに「お知らせ」欄を新設（未デプロイ）

### やったこと
- トップページ（`index.html`）のヘッダー直下・ヒーローのすぐ後ろに、新しい「お知らせ」セクション
  （`id="notice"`）を追加。ナビゲーション（PC・スマホ両方）にも「お知らせ」を先頭に追加した。
  - 見出し：「三豊市スポーツ協会バウンドテニス部主催　『バウンドテニス教室』開催のお知らせ」
  - 案内文＋申込みフォーム（`bt-kyoushitsu`）へのボタンを設置。
  - 案内ポスターとして、ロイさんの指定した画像
    （`Desktop\ショートカット\R7年度　三豊市スポーツ協会書類\R7年度　バウンドテニス教室\バウンドテニス教室チラシ2-20250918-01.JPG`）
    を `image/bt-kyoushitsu-flyer-kari.jpg` としてコピーして掲載。**「仮」のラベル付き**
    （ポスター右上に「案内ポスター（仮）」の吹き出し、本文にも「正式版が完成し次第、差し替えます」の注記）。
  - 元画像は1.9MB（3136×4465px）と重かったため、`ffmpeg` で幅900pxに縮小・再圧縮して256KBに軽量化した
    （他の画像素材は概ね数百KB以下のため、それに合わせた）。

### わかったこと・つまずいたこと
- **貼り付けたポスター画像の日時・会場・対象学年が、既存の申込みフォームと食い違っている。**
  - ポスター：11/1・11/8・11/15・11/22（すべて土曜）／吉津小学校／小学4年生以上／受付締切10/24
  - 申込みフォーム（`bt-kyoushitsu.html`・`worker.js`の`BT_DATES`）：10/10・10/17・10/24・11/7
  - ポスターはロイさんの指示どおり「仮」として貼っただけなので、日付や条件をポスターに合わせて
    書き換えることはしていない。正式版のポスターが決まった時点で、**開催日が変わるなら
    `bt-kyoushitsu.html` と `worker.js`（`BT_DATES`）を両方直す必要がある**（対象学年・会場を
    フォームに載せるかどうかも、そのとき合わせて判断が要る）。
  - お知らせ欄の案内文では、開催日を具体的に書かず「詳しくは申込みフォームでご確認ください」と
    誘導する形にして、この食い違いが表に出ないようにした。

### 次にやること
- **ロイさんの指示で今回はデプロイしていない。** 内容を確認のうえ、良ければ
  `dev/tool/senior-site-api` の要領（`roys-channel`側は`wrangler.cmd pages deploy .`
  もしくはGitHub push）で公開する。
- ポスター正式版が決まったら、画像差し替え＋（必要なら）日程の食い違いを解消する。

### 動作確認
- ローカルの一時サーバー（`http://localhost:8892/roys-channel/index.html`、
  `dev/tool/.claude/launch.json` に `site-preview-2` として追加）で、お知らせ欄の見た目・
  レイアウト（PC幅）を確認。ナビゲーションの「お知らせ」リンクでスクロールすることも確認。
  スマホ幅・実際の本番デプロイでの見た目は**未確認**（デプロイ自体していないため）。

---

## 2026-08-16（日）その2 — CC・受付完了メール・D1保存・管理ページを追加してデプロイ

外出中のロイさんに代わり、自動で進めた分。

### やったこと
- **CC機能を追加。** フォームに「確認メールを追加で送る宛先（CC）」欄（任意・カンマ区切りで複数可）を追加。
  `bt-kyoushitsu.html` と `worker.js` の両方を対応させた（メール形式でないもの・本人と同じもの・
  6件目以降は自動で除外。上限は `BT_CC_MAX = 5`）。
- **受付完了メールを追加。** 申込み時にメールアドレスが入っていれば、申込者ご本人へ
  「お申込みを受け付けました」メールを自動送信するようにした（`worker.js` の `handleBtApply` 末尾）。
  前回の日誌にあった「自動返信は送れない」という制約は、**送信元を変えずに"申込者宛の新規メール"として送る形なら問題なかった**
  （送れないのは「返信」だけで、新規の送信は元から可能だった）。
- **D1に申込み内容を保存。** `senior-site-analytics`（既存のD1）に `bt_applications` テーブルを新規作成し、
  申込みのたびに1行保存するようにした。メール送信が本体で、D1保存は失敗しても申込み自体は失敗にしない設計。
- **確認用の管理ページを新規作成**（`bt-apply-admin.html`）。管理キーを入力すると、保存済みの申込み一覧を
  新しい順に表で見られる。`noindex,nofollow` 付きでどこからもリンクしていない、住所を直接開く形。
  - 認証は `ADMIN_KEY`（`wrangler secret put` で設定ずみ・このファイルには書かない）。
    一致しなければ `/bt-applications` は404を返す（存在を悟らせない）。
  - キーは初回入力後、そのブラウザの localStorage に覚えさせる（毎回入力しなくていい）。
- **Workerをデプロイした**（`dev/tool/senior-site-api` で `wrangler.cmd deploy`）。
  `/bt-apply`・新規の `/bt-applications` とも本番で動作確認ずみ。
- **本番で実際にテスト送信して確認**（`roy.s1105@gmail.com` 宛、テスト後にD1から削除ずみ）。
  - `/bt-apply` → `{"ok":true}` を確認。
  - `/bt-applications?key=…` → 正しいキーで一覧が返る／間違ったキーで404、両方確認。
- **`roys-channel` を GitHub に push**（Cloudflare Pages が自動ビルドする方式のため）。

### わかったこと・つまずいたこと
- **PowerShellのcurlはJSONの日本語を含む引数でエスケープが崩れる。** `curl -d '{"name":"テスト"...}'`
  を直接打つと `missing_fields` になった。ファイルに書いてから `--data-binary @file` で渡すと問題なかった。
  次に同じテストをするときはこの方法を使う。
- **ADMIN_KEYはこのファイル・DEVLOGのどちらにも書いていない。** ロイさんが忘れた場合は
  `dev/tool/senior-site-api` で `wrangler.cmd secret put ADMIN_KEY` から作り直せる（新しい値になる）。
  完了メールに書いて送っている。

### 次にやること
- **Cloudflare Pages が今回の push を実際にビルドしたか確認**（前回8/15にビルドが始まらない不具合があったため）。
  `https://roys-channel.pages.dev/bt-kyoushitsu` と `.../bt-apply-admin` を開いて実物を見るのが確実。
- トップページ（`index.html`）から、このフォーム／管理ページへのリンクを張るかどうか、まだ決めていない。
- 会場・時間・参加費・持ち物は仕様書に無いので未掲載のまま。

### 動作確認
- Worker本番（`roy-senior-site-api.roy-s1105.workers.dev`）… `/bt-apply` 送信・D1保存・
  `/bt-applications` 認証つき一覧表示、すべて実測ずみ。
- `roys-channel.pages.dev` 側の実際の反映（Cloudflare Pagesのビルド）は **push直後のため未確認**。

---

## 2026-08-16（日）— バウンドテニス教室 申込みフォームを新設

### やったこと
- **`bt-kyoushitsu.html` を新規作成**（Roy's Channel 直下の専用ページ。公開後の住所は
  `https://roys-channel.pages.dev/bt-kyoushitsu` ）。
  もとの仕様書は `Desktop\RoyWorkingFolder\バウンドテニス\三豊市スポーツ協会\バウンドテニス教室申込みフォーム_仕様まとめ.md`。
  - 色・書体・ボタンの形は `index.html` と同じもの（`--navy` `--red` `--line`、Zen Maru Gothic）を写して合わせた。
    ただし**1枚で完結する独立ページ**なので、CSSはこのファイルの中に持たせている（index.html には手を入れていない）。
  - 入力欄8つ：お名前／フリガナ／年齢（年代・選択式）／電話番号／メール／経験（3択）／
    参加希望日（4日程・複数選択）／質問・ご要望（任意）。
  - **検索よけに `<meta name="robots" content="noindex">` を入れている**（申込み専用ページのため）。
    検索に出したくなったら、この1行を消す。
- **バックエンドに `/bt-apply` を追加**（`dev/site/senior-site/material/worker.js`）。
  お問い合わせの `/contact` と同じ Worker・同じ Resend を使い回している。**まだデプロイしていない。**
  - **メールの届け先は `BT_MAIL_TO` の1行だけ直せば変えられる**（worker.js 内、`handleBtApply` の上）。
  - 開催日は `BT_DATES` に持たせ、**その中の日付しか受け付けない**。日付を変えるときは
    worker.js と `bt-kyoushitsu.html` のチェックボックス、**両方**を直すこと。
- 高齢の方が使う前提で、押しやすさを優先した。
  - 選択肢は**枠ごと押せるカード**（高さ60px）。文字は入力欄16.5px。
  - スマホでは選択肢もボタンも幅いっぱい（297px）。

### わかったこと・つまずいたこと
- **仕様書の「曜日は仮」は、そのままで正しかった。** 2026年の
  10/10・10/17・10/24・11/7 は**4日とも土曜日**（`Get-Date -Format ddd` で確認）。直す必要なし。
- **申込者ご本人への自動返信は、いまの仕組みでは送れない。**
  Resend の送信元が `onboarding@resend.dev`（Resend のお試し用アドレス）なので、
  **アカウントの持ち主宛にしか届かない**決まり。自動返信を付けるには、
  Resend に**自分のドメインを登録して認証する**必要がある。
  → 今回は「受け取ったメールにそのまま返信すれば申込者に届く」形（`reply_to`）にした。
- **入力欄の文字は16px以上にしないと、iPhoneで勝手に拡大される。** 16.5pxにしてある。
- **仕様書に無かったので、会場・時間・参加費・持ち物は書いていない。** 必要なら足す。

### 次にやること
1. **メールの届け先を決めて `BT_MAIL_TO` を直す**（ロイさんの指示待ち。いまは仮に `roy.s1105@gmail.com`）。
2. `dev/tool/senior-site-api` で `wrangler.cmd deploy` → `/bt-apply` を公開する。
3. `roys-channel` を push（Cloudflare Pages がビルド）→ 実物で1件送信して、メールが届くか確認する。
4. トップページ（`index.html`）から、このフォームへのリンクを張るかどうか決める。
   いまは**どこからもリンクしていない**（住所を直接お伝えして使う形）。
5. 会場・時間・参加費・持ち物を載せるかどうか決める。

### 動作確認
- ローカル（`http://localhost:8765/roys-channel/bt-kyoushitsu.html`）で確認。
  - **必須チェック9通りすべて**、狙ったメッセージが出ることを確認
    （未入力→順に指摘／短すぎる電話→注意／`abc` のようなメール→注意）。
  - `fetch` を偽物に差し替えて**送信の中身を実測**。宛先は `…workers.dev/bt-apply`、
    中身は8項目＋選んだ日付2件が正しく入っていた。成功後にフォームが隠れ、お礼の画面に切り替わることも確認。
  - 幅1265px／幅375px（スマホ）とも**横のはみ出しゼロ**（`scrollWidth = clientWidth`）。
  - 見た目はヘッドレスEdgeで撮って目視（ブラウザ枠はスクリーンショットが撮れないため）。
- **本番の送信テストはしていない**（Worker が未デプロイ、届け先も未確定のため）。

---

## 2026-08-15（土）その3 — push したが、**Cloudflare がビルドを始めない**（未解決）

### やったこと
- `main` に push（`0fe86dc` → `7fce33c`）。**GitHub には入っている**（push 成功を確認）。
- リンクを `.html` 無しの形に変更（Cloudflare Pages はその形で配るため）。

### つまずいていること（**未解決・ロイさんの操作が要る**）
- **`roys-channel.pages.dev` は古い版（`88c7756`）のまま。** 新しいコミットが公開されない。
  - Cloudflare のデプロイ履歴は**1時間前の1件だけ**。**失敗した記録すら無い**
    → ビルドが**始まってすらいない**（失敗ではなく、通知が届いていない）。
  - 同じタイミングで push したシニアサイトは**数秒でデプロイされた**。→ Cloudflare 全体の不調ではない。
- **いちばん疑わしいのは、GitHub側のアプリのリポジトリ許可。**
  「Cloudflare Workers and Pages」アプリが `roys-channel` を見られていない可能性。
  - GitHub → Settings → Applications → **Cloudflare Workers and Pages** → Configure
    → Repository access に **`roys-channel` を追加**（または All repositories）
  - そのあと Cloudflare → roys-channel → Deployments → **Retry / Create deployment**
- **今の状態でも表示は壊れていない。** 古い版のリンクは `roys1105.github.io/roys-senior-it/…` を
  指しているが、そこは新住所へ転送されるので、行き先には着く。

### 動作確認
- `roys-channel.pages.dev` … **古い版**（canonical 無し・リンクが github.io のまま）を実物で確認
- GitHub 側 … `7fce33c` まで反映ずみ

## 2026-08-15（土）その2 — 公開先を Cloudflare Pages に移す準備

### やったこと
- **方針**（ロイさんの裁定）：2つのサイトは**別々のサイト**として公開し、相互リンクで繋ぐ。
  公開は **Cloudflare Pages**、**GitHub は履歴の保管用**（昔に戻したいときの手段）。
  - Roy's Channel … `https://roys-channel.pages.dev`（作成ずみ・GitHub連携あり）
  - シニア相談室 … `https://roys-senior-it.pages.dev`（作成ずみ・GitHub連携あり）
- **【追記】シニア側のURLが `roys-senior-it-git` → `roys-senior-it` に変わった**（プロジェクトを作り直したため。
  **Cloudflare Pages は名前をあとから変えられない**）。`index.html` のリンクも全部それに合わせた。
- **`index.html` を直した（4か所＋2つ）。**
  - シニアサイトへのリンク**4か所すべて**を `roys1105.github.io/roys-senior-it/…` →
    `roys-senior-it.pages.dev/…` に変更。あわせて **`target="roy-senior"` / `target="roy-main"` を全部外した**。
  - **タブの「名札」（`window.name = 'roy-main'`）を廃止**。別ドメインをまたぐとブラウザが名札を消すため、
    この方式はもう使えない。行き来は**同じタブで移動**に統一（`target` を付けない）。
  - `<head>` の先頭に**旧住所からの転送**を追加。`location.hostname === 'roys1105.github.io'` のときだけ
    `roys-channel.pages.dev` の同じ場所へ飛ばす（`location.search` と `#` も引き継ぐ）。
    **同じファイルが GitHub Pages にも出てしまうので、host を見て分岐している。**
  - `<link rel="canonical" href="https://roys-channel.pages.dev/">` を追加（検索での重複よけ）。

### わかったこと・つまずいたこと
- **2つのサイトが別ドメインになると、これまでの仕掛けが2つとも効かなくなる。**
  1. タブの名札（`window.name`）… ドメインをまたぐと消える決まり → **廃止した**
  2. 特商法ページの「来た方向」判定 … 別ドメインだと **referrer はホスト名までしか読めない**（パスは消える）
     → シニア側で `?src=` の印を主、**ホスト名（`roys-channel`）を予備**に切り替えた
- **1つのリポジトリが2か所（GitHub Pages と Cloudflare Pages）に出る。**
  だから転送も LIVE_HOSTS も「**今どっちのホストで開かれているか**」を見て書く必要がある。
  ベタ書きの `if (…) location.replace(…)` は片方でしか動かないように必ず host で囲むこと。
- ローカル（`localhost:8765`）で確認したかぎり、転送も名札廃止も**手元では発火しない**（host が違うため）。
  ここは意図どおり。

### 次にやること
1. ~~Cloudflare で Pages プロジェクト `roys-channel` を作る~~ → **ロイさんが作成ずみ**（GitHub連携あり）。
2. **両リポジトリを push する**（ロイさんの合図待ち）。push した時点で新しい住所に切り替わる。
3. 公開後、実物で確認：旧URL → 新URLの転送、相互リンク往復、特商法の「戻る」先。
4. note の記事など**外に出したリンクは旧URLのまま**。転送で繋がるが、新しく書くものは新URLで。

### 動作確認
- ローカル静的サーバ（`python -m http.server 8765`）で `roys-channel/` を開いて確認：
  - シニアへのリンク4本すべて `https://roys-senior-it.pages.dev/…`、**target は全部なし**
  - `window.name` は空（名札廃止を確認）
  - canonical は `https://roys-channel.pages.dev/`
  - 画像は全部 200。404 はシニア側の `.image-slots.state.json` 1件だけ（元からある内部ファイル・無害）
- **ライブでの確認は未実施**（Cloudflare のプロジェクトがまだ無いため）。

## 2026-08-15（土） — 講座「田舎暮らしのまま、独りで稼ぐ教科書」の紹介欄を追加

### やったこと
- **講座の紹介セクションを新設した**（`index.html`／`<section class="kouza" id="kouza">`）。
  置き場所は「シニア相談室への導線」セクションのすぐ下、プロフィールの上。
  - 左に表紙（`image/cover-inaka.jpg`）、右に説明文＋目次（全7回）＋ボタン、の2カラム。
  - 表紙の左上に「全7回・無料」の赤いバッジを少し傾けて重ねた。
  - ボタン「講座のページを見る →」の飛び先は
    `https://roys1105.github.io/roys-senior-it/material/inaka-hitori-kasegu.html`
    （別タブ。`target="_blank" rel="noopener"`）
  - 説明文と目次7本の見出しは、飛び先の講座ページから写して合わせた（想像で書かない）。
- **CSSを追加**（`.senior-cta .note` の下、profile の直前）。
  `.kouza` `.kz-grid` `.kz-cover` `.kz-badge` `.kz-lead` `.kz-sub` `.kz-list` `.kz-cta`。
  既存の `--navy` `--red` `--line` などの色変数と `.btn .btn-primary` をそのまま使っている。
- **メニューに「講座」を追加**（パソコン用 `nav.pc` とスマホ用 `nav.sp` の両方、`#kouza` へ）。
- 画面幅への対応も追加。
  - 900px以下：2カラム→1カラム、表紙は320pxで中央寄せ
  - 640px以下：目次を1列に、ボタンを幅いっぱい（420pxまで）

### わかったこと・つまずいたこと
- **メニューのリンクを増やすときは、2つのメニュー両方を直すこと。**
  パソコン用とスマホ用で字下げが違うので、一括置換だと片方しか当たらない（実際に1回外した）。
- **メニューは7項目でも横幅は足りる。** 700px幅で brand の右端177px、nav が191〜678px。
  はみ出しは出ていない。640px以下ではハンバーガーに切り替わるので問題なし。
- **ブラウザ枠で `dev` 配下のHTMLを開くと data: の静的スナップショットになり、
  相対パスの画像が読み込めない**（`naturalWidth` が 0 になる）。画像を data URI に
  埋め込んでも読めなかった。スクリーンショットも「ペインが非表示」で撮れない。
  → 見た目の確認は、要素の実寸をJavaScriptで測る方法に切り替えた。
  実際の見え方を目で確かめたいときは、`index.html` を普通のブラウザで開くのが早い。
- **写真の点検結果（このサイト全体）**：参照している画像22件はすべて存在し、欠けはゼロ。
  `image/` には使っていない画像が16枚残っているだけ（art-*.jpg、photo-touring.jpg など）。
  壊れたリンクも無し。`roy-avatar.png` だけ `alt=""` だが、これは飾りなので問題ない。

### 追記：シニア相談室の3枚カードから、講座のカードを外した
- 上の紹介セクションを作ったことで内容が重複したため、
  「シニア相談室への導線」欄の3枚カードから
  **「田舎暮らしのまま、独りで稼ぐ」のカードを削除**し、2枚（スマホ教室／脳いきいき手帳）にした。
- 2枚でも間延びしないよう `.s-cards` を調整。
  - `repeat(3,1fr)` → `repeat(2,1fr)`、`gap` 18px → 20px
  - `max-width:720px; margin:0 auto 38px;` で欄ごと中央寄せ
  - 900px以下（1カラムになる幅）では `max-width:460px` で中央に置く
- `image/cover-inaka.jpg` は下の紹介セクションで引き続き使っている。
- **そのあと「もう少し大きく」の指示を受けて、2枚をさらに一回り大きくした。**
  - `.s-cards` の `max-width` 720px → **880px**、`gap` 20px → 24px
  - `.s-card` の余白 16/16/20 → **20/20/26**、角丸 20px → 22px
  - `.s-card .thumb` の高さ 120px → **170px**（表紙もアプリ画面も大きく見える）
  - 見出し 15.5px → **17.5px**、本文 13px → **14px**、タグ 11px → 12px
  - 1カラムになる幅（900px以下）の `max-width` も 460px → **520px**
  - 結果：カード1枚 350×293 → **428×368**、画像枠 318×120 → **386×170**

### 追記：シニア相談室へのリンクで、同じタブを使い回すようにした
- ページの末尾のスクリプトに処理を追加。`roys1105.github.io/roys-senior-it` を
  含むリンク（**4か所**：紹介欄のボタン、講座のボタン、フッター2つ）が対象。
- 仕組み：クリックしたら `window.open('', タブ名)` を呼ぶ。
  - 同じ名前のタブがすでにあれば、それが返ってくる。中身は別サイトなので
    `location.href` を読もうとすると例外が出る。→ **すでにあるタブと判断して、切り替えるだけ**
    （読み込み直さないので、見ていた場所が保たれる）
  - 無ければ真っ白なタブが返る（`location.href` が `about:blank`）。→ そこにURLを読み込む
- **タブの名前はページごとに分けている**（トップ＝`roys-senior-`、
  講座＝`roys-senior-material-inaka-hitori-kasegu-html`）。
  同じ名前にすると、講座のボタンを押しても既に開いたトップに切り替わるだけになってしまう。
- **できないこと**：ブラウザは「このページから開いたタブ」しか見つけられない決まりなので、
  ロイさんが手でURLを打って開いたタブは見つけられない。その場合は新しいタブが開く。
  ポップアップを止められている環境では、いつもどおり普通に別タブで開く（`window.open` が
  `null` を返したときは何もせず、リンク本来の動きに任せている）。

### 追記：講座は「メインサイトのタブ」で開くようにした
- **不具合**：講座を開くと、**シニア相談室を見ていたタブが講座に置き換わっていた。**
  講座ページのURLがシニアサイトの下（`roys-senior-it/material/…`）にあるため、
  「シニアサイトのページは全部 `roy-senior`」と名札を付けてしまったのが原因。
  **URLの置き場所と、どのタブで見せたいかは別の話**だった。
- **直し方**：講座ページだけ名札を **`roy-main`** にし、Roy's Channel の講座ボタンも
  `target="roy-main"` に変えた。講座は Roy's Channel から見てもらうものなので筋が通る。
- これでタブは2枚のまま。**メイン（タブ1）で講座を開き、「トップに戻る」でメインに戻る。
  シニア相談室のタブ（タブ2）は触られない。**
- 名札の割り当て（2026-08-15 時点）
  | ページ | 名札 |
  |---|---|
  | Roy's Channel トップ | `roy-main` |
  | 講座「田舎暮らしのまま、独りで稼ぐ教科書」 | `roy-main` |
  | シニアサイト トップ／スマホ教室／脳いきいき手帳 | `roy-senior` |
  | 特商法（両方からリンクされている） | 来た方向による。名札は上書きしない |

### 追記：タブ2枚で行き来する形にした（名札方式）★仕組みの説明
- 目指した動き：**メイン（タブ1）⇄ シニア（タブ2）**。3枚目を作らず、押すたびに相手のタブに切り替わる。
- **やり方：各ページが自分のタブに「名札」を付ける。**
  ```js
  window.name = 'roy-main';    // Roy's Channel のページ
  window.name = 'roy-senior';  // シニアサイトのページ
  ```
  そのうえで、リンクに `target="roy-senior"` / `target="roy-main"` を付ける。
  **名札の付いたタブがあれば、ブラウザはそこへ切り替える。無ければ新しく開く。**
- **なぜ前はうまくいかなかったか**：`target` だけだと「そのページが開いたタブ」しか見つけられない。
  最初のタブ（ロイさんが自分で開いたタブ1）は名前が無いので、戻るときに見つけられず
  3枚目ができてしまっていた。**`window.name` で自分から名乗れば、そのタブも見つけてもらえる。**
- **2つのサイトが同じ `roys1105.github.io` だから使える手**。
  別ドメインに分けると、行き来のたびに名札が消される（ブラウザの決まり）ので、この方法は使えなくなる。
- 名札を入れたファイル：Roy's Channel の `index.html`、シニアサイトの `index.html`・
  `tokushoho.html`・`material/` の3ページ（全部で6ファイル）。
- `target` を付けたリンク：Roy's Channel 側4か所（→roy-senior）、
  シニアサイト側4か所（→roy-main。トップのボタン＋講座ページの3つ）。
- **確認できたところまで**：両サイトで名札とtargetが正しく入っていることをライブで確認した。
  タブが実際に2枚で行ったり来たりするかは、**ブラウザ枠がスクリプトからのタブ生成を止めるため
  ここでは試せない**。ふだんのブラウザでの確認が要る。

### 追記：2つのサイトの行き来を、全部「同じタブ」にした（タブを使い回す仕組みは廃止）
- **タブに名前を付けて使い回す方法はやめた。** ブラウザは「そのページ自身が開いたタブ」しか
  見つけられない決まりで、**手でURLを打って開いたタブがあると、結局タブが増えてしまう**ため。
  同じタブで移動すれば、タブは絶対に増えない。これが確実な答えだった。
- Roy's Channel 側：シニア相談室へのリンク**4か所すべて**から `target="_blank" rel="noopener"`
  を外した（紹介欄のボタン、講座のボタン、フッターの2つ）。
  `seniorTabName()` とクリック処理のスクリプトは**削除**。`data-same-tab` の目印も不要になったので消した。
- シニアサイト側：「もう一つの顔」の Roy's Channel ボタンから `target="roys-channel"` を外した。
- YouTube・note へのリンクは**別タブのまま**（よそのサービスなので、戻れなくなると困る）。
- 戻り道：行き先のページに「トップに戻る」があるほか、ブラウザの戻るボタンでも戻れる。

### 追記：講座のボタンを「同じタブ」に変えた
- 「講座のページを見る →」は別タブで開いていたが、**同じタブで開く**ようにした。
  講座ページの「トップに戻る」で、そのままここへ戻ってこられる形にするため。
  - `target="_blank" rel="noopener"` を外し、代わりに **`data-same-tab="1"`** を付けた。
  - タブを使い回すスクリプトの対象から外すため、セレクタを
    `a[href*="…roys-senior-it"]:not([data-same-tab])` に変更。
    シニア相談室のボタンとフッターの2つは、これまでどおりタブを使い回す。
- **「別のタブで開きます」の文言を、両サイトから消した。**
  - Roy's Channel：講座の注記→「いまは無料で公開中です」、
    相談室の注記→「スマホ・タブレット・パソコンのどれでもご覧いただけます」
  - シニアサイト：「もう一つの顔」のRoy's Channelボタンの下の注記は、まるごと削除
  - 脳いきいき手帳の教材ページにある「※新しいページ（別のタブ）で開きます。閉じれば…」は
    **そのまま残している**（アプリを開く話で、今回の行き来とは別のものなので）
- 講座ページ側（シニアサイトの `material/inaka-hitori-kasegu.html`）の文言も元に戻した。
  「Roy's Channel に戻る」→**「トップに戻る」**、
  「Roy's Channel のトップページに戻る」→**「トップページに戻る」**。飛び先は Roy's Channel のまま。

### 追記：講座の表紙の下に「見られた回数」を出した
- 表紙の下に `<p class="kz-count" id="kz-count">` を置き、
  シニアサイトと同じ集計API（`roy-senior-site-api.roy-s1105.workers.dev/page-stats`）から
  **講座ページ（`/materials/inaka-hitori-kasegu.html`）の閲覧回数**を読んで表示する。
  表示は「この講座が見られた回数　今日 ◯回／のべ ◯回」。
- **数えているのはシニアサイト側の仕組み**（講座ページを開いたときに記録される）。
  こちらは読むだけなので、**この欄を見ても数字は増えない**。
- 今日ぶんは `results` から、のべは `totals` から取る。`data.totals || data.results` と
  書いてあるので、Cloudflareの Worker がまだ古くても壊れない。
- 読み込み前は「今日 —回／のべ —回」と出しておき、位置がずれないようにした。

### 追記：公開した（GitHub Pages）
- **ライブURL：https://roys1105.github.io/roys-channel/**
- リポジトリ：`git@github.com:roys1105/roys-channel.git`（Public、`origin` として設定ずみ）
- 公開方式：GitHub Pages「Deploy from a branch」＝ `main` / `/ (root)`。
  **これ以降は `git push origin main` するだけでライブに反映される**（ビルドに30秒ほど）。
- `gh` コマンドをこのPCに入れた（winget、v2.97.0。場所は `C:\Program Files\GitHub CLI\gh.exe`）。
  PATHが通っていないシェルからは**フルパスで呼ぶ**こと。
- 認証は「デバイスフロー」で通した。gh の `auth login --web` は、この環境（キー入力ができない）
  では固まって進まない。代わりに `https://github.com/login/device` のコードを出して
  ロイさんに承認してもらい、得たトークンを `gh auth login --with-token` に渡した。
  トークンを書いた一時ファイルは、その場で削除ずみ。

### 追記：GitHubへの公開の準備をした（公開そのものは未完了 → 上記のとおり完了）
- シニアサイトから戻ってくるボタンを作るために、**このサイトを公開することにした**。
- `git init`（ブランチ `main`）して初回コミットまで済ませた。41ファイル。
- `.gitignore` を新規作成し、`_backup/`（18MBの旧・下書きHTML）を除外した。
- **まだGitHubには上がっていない。** この環境には `gh` コマンドが無く、
  リポジトリの新規作成とPagesの有効化はブラウザ側の操作が必要なため。

### 次にやること（2026-08-15 の作業おわり時点）
- **サイトは公開ずみ**（https://roys1105.github.io/roys-channel/ ）。
  シニアサイト側のボタンも公開ずみで、両サイトはつながっている。
- **ロイさんの確認待ち**：ふだんのブラウザで「メイン→シニア→メイン→シニア」と押して、
  **3枚目のタブができないか**を見てもらう。こちらのブラウザ枠では試せない。
  細かい段取りは `dev/site/senior-site/DEVLOG.md` の「次にやること」に書いた。
- Instagram・X をやっているなら「発信」の枠に追加する（今はURLが分からないので入れていない）
- キャンプの写真が見つかっていない。あれば「好きなこと」に1枠足す

### 動作確認
- 幅1280px：2カラム（表紙320px＋本文654px）、目次は2列、はみ出しなし。
- カード欄（2枚・拡大後）：1280px で 428×368 が2枚とも同じ大きさで中央
  （193〜1073px／親は103〜1163px）、画像枠は386×170。
  389px では 345×366・画像枠303×170、横のはみ出しなし。
- 幅700px：メニュー7項目が収まる、はみ出しなし。
- 幅389px：1カラム、目次1列、ボタン幅330px、はみ出しなし。
- ボタンの飛び先が講座ページのURLになっていること、目次が7件あることを確認。
- 表紙の実物の見た目だけは未確認（上記の理由でプレビューに画像が出ないため）。
- タブの使い回し：`window.open` を差し替えた偽物に入れ替えて、実際にクリックを流して確認した。
  1回目＝新しいタブを開いてURLを読み込む→切り替え／2回目＝**読み込み直さず切り替えるだけ**／
  講座のボタン＝別の名前のタブを開く。対象リンクは4か所とも拾えている。

---

## 2026-08-02（日）その4 — 「3つの顔」No.03 の写真差し替え

### やったこと
- `index.html` 488行目（`.face` の No.03「バウンドテニスの世話役」）の写真を差し替え。
  - `image/photo-bt-taikai.jpg` を新規作成
    ← 元：`D:\Roy.S\Pictures\iPhone\20250525\IMG_9750.jpg`（5712×3213／大会の開会式）
  - **1600×727（比率2.2）** に切り出し。元画像は天井と手前の床が広く空いていたので、
    参加者の列が枠の中央に来るよう **上840px・下32px をカット**、左右も少し詰めた。
  - `object-position:center 45%`、alt は「体育館に整列したバウンドテニス大会の参加者たち」

### わかったこと・つまずいたこと
- `.face .ph` は **高さ168px 固定**（スマホは200px）で幅だけ可変なので、
  スロットの比率は **1.6〜2.2 くらいまで変動する**。画像は 2.2 前後の横長にしておくと、
  どの幅でも `object-fit:cover` が横方向を少し削るだけで済み、上下が切れない。
- 旧 `photo-bt2.jpg` は **「好きなこと」欄（615行目）でまだ使っている**ので消さないこと。
  今回あえて別ファイル名にしたのはそのため。

### 次にやること
- 特になし

### 動作確認
- ヘッドレス（1200px幅・3カラム表示）で該当カードを描画して目視。参加者の列が枠いっぱいに収まっている。

---

## 2026-08-02（日）その3 — TOURINGカードの写真差し替え＋本文修正

### やったこと
- **写真を差し替え**（`index.html` 602行目）
  - `image/photo-bike-helmet.jpg` を新規作成
    ← 元：`D:\Roy.S\Pictures\iPhone\20240316\098.JPG`（3024×4032 の縦写真）
  - 横位置のカードに合うよう **上から121px の位置で 3024×2016（3:2）に切り出し**、
    1600×1067 / quality 84 で保存（344KB）。バイクと赤黒ヘルメットが両方入る構図。
  - `object-position` は `center 42%` → **`center center`**（画像とカードの比率がほぼ同じため）
  - alt も「並木道に停めた黒い大型バイクと、赤黒のヘルメットを提げて歩くロイ」に更新
- **本文を修正**（610行目）
  「赤いキャップとサングラス、赤黒のヘルメット。〜」
  → **「赤黒のヘルメットに黒い大型バイク。走っているのを見かけたら、たぶんロイです。」**

### わかったこと・つまずいたこと
- 旧 `photo-touring.jpg`（1200×1600 の縦写真）は**残してある**。戻したくなったら
  602行目の `src` と `object-position:center 42%` を書き戻せばよい。
- iPhone の写真は **EXIF の回転情報**を持っているので、PIL で開いたら
  `ImageOps.exif_transpose()` を必ず通すこと。忘れると横倒しで保存される。
- `.like-art` は `min-height:320px` で、高さは**隣の文章の量で決まる**。
  だから画像は 3:2 くらいの横長にしておいて、細かいズレは `object-fit:cover` に任せるのが楽。

### 次にやること
- 特になし

### 動作確認
- ヘッドレス（1200px幅）で該当カードを描画して目視。写真・本文とも意図どおり。

---

## 2026-08-02（日）その2 — ヒーローの吹き出しをマンガ風にリデザイン

### やったこと
- `.hero-bubble`（`index.html` 125行目あたり）を作り直した。
  - 角丸を **四隅バラバラ**（`border-radius:30px 26px 32px 24px / …`）にして手描きっぽく
  - 枠線 2px → **2.5px**、影を **オフセット影＋ふんわり影の2枚重ね**に
  - しっぽを、**外側＝線の色／内側＝白の三角を2枚重ね**て「フチ付き」にした
    （`::before` が外、`::after` が内。左右の border-width を非対称にして斜めのしっぽに）
  - 4.2秒でふわふわ上下する `@keyframes bubble-bob` を追加
- 中の文字を `<span class="bb-text">やあ、<em>ようこそ</em><b>！</b></span>` に分割してデザイン。
  - 「ようこそ」= 赤＋下に薄赤のライン、「！」= 赤・大きめ・9度傾け、先頭に小さな「✦」

### わかったこと・つまずいたこと
- **フチ付きのしっぽは、三角を2枚重ねるのが確実。** `box-shadow` は
  `border` で作った三角形（擬似要素）には効かないので、しっぽに影は付かない。
  今回は本体の影だけで十分自然に見えた。
- **Browser ペインが非表示だとスクリーンショットが撮れない**
  （"the Browser pane is not displayed" エラー）。そのときは
  `msedge.exe --headless=new --screenshot=...` で撮って PIL で切り出すと確認できる。
  出力先は **Windows の絶対パス**で渡さないと「アクセスが拒否されました」になる。
- `prefers-reduced-motion:reduce` のときはアニメを止めるようにした（サイト初の対応）。

### 次にやること
- 特になし

### 動作確認
- ヘッドレスで **760px幅** と **375px幅（スマホ）** の両方を描画して目視。
  吹き出し・しっぽ・文字とも意図どおり。スマホでも画面からはみ出していない。

---

## 2026-08-02（日）— ヒーロー画像の縦の間延びを修正

### やったこと
- `index.html` の `.hero-figure img`（line 124）に **`height:auto` と `aspect-ratio:760/990`** を追加。
- 同じ画像の `<img>` タグ（line 411）の `width`/`height` 属性を、実寸に合わせて
  **620×824 → 760×990** に修正。

### わかったこと・つまずいたこと
- 原因は **`height` 属性が「そのままCSSの高さ」として効いていた**こと。
  `.hero-figure img` は `width:100%` だけで `height:auto` が無かったため、
  幅は440pxに縮むのに高さは824pxのまま → 縦に伸びて見えていた。
- グローバルの `img{max-width:100%;display:block;}`（line 30）にも `height:auto` が無い。
  今後 `<img>` に `height` 属性を書くときは、CSS側で `height:auto` を必ずセットすること。
  （※ 現状 `height` 属性を持つ `<img>` はこの1か所だけなので、他は無事）

### 次にやること
- 特になし（気になるなら line 30 に `height:auto` を足して全体で予防してもよい）

### 動作確認
- ローカルサーバ（`roys-channel-preview` / port 8938）で確認。
- 表示サイズ **418×545**、元画像 **760×990** → 比率はどちらも **0.7677 で一致**。間延びは解消。

---

## 2026-07-31（金）その3 — 写真がPC内で見つかったので差し替え

### やったこと
「その2」で見つけられなかった写真を、**コンタクトシート（一覧画像）を自動生成する方法**で
探し出し、4か所すべてを実写に差し替えた。

- **「バイクとキャンプの人」の枠** … イラストをやめて**実写2枚を左右に並べた**（`.ph.duo`）
  - `photo-bike.jpg` ← `202307_a\IMG_5486.JPG`（海沿いのガードレール脇に停めた愛車）
  - `photo-camp.jpg` ← `202204_a\IMG_4462.JPG`（タープ＋焚き火台＋薪）
- **ツーリングの枠** … `art-touring.jpg`（イラスト）→ **元になった実写**に差し替え
  - `photo-touring.jpg` ← `202403_a\IMG_E6710.JPG`
    （木の柵・葉の落ちた並木・砂利道。イラストと同じ構図・同じアングル）
- **バウンドテニスの枠2か所** … 女性のイラスト（`art-bt-coach.jpg`）→ **実写**に差し替え
  - `photo-bt2.jpg` ← `202511_a\IMG_0619.JPG`（体育館に張った緑のコート。人は遠景）
- **「うどん県は、いいところです」の枠** … 桜1枚 → **春夏秋冬の4枚を2×2で配置**（`.like-art.quad`）
  - 春 `season-spring.jpg` ← `202504_a\IMG_E9217.JPG`（提灯のならぶ桜並木）
  - 夏 `season-summer.jpg` ← `202507_a\IMG_0056.JPG`（松林ごしの瀬戸内海）
  - 秋 `season-autumn.jpg` ← `202410_a\IMG_E7903.JPG`（うろこ雲の夕焼け）
  - 冬 `season-winter.jpg` ← `202412_a\IMG_E8404.JPG`（波の荒い冬の海岸）
  - 各写真の左下に季節ラベルを重ねた（`figcaption`）

### わかったこと・つまずいたこと
- **8,500枚を探す方法＝コンタクトシートを作って一覧で見る。**
  作業用スクリプトは scratchpad の `sheet.ps1`（フォルダ単位）と `sheet2.ps1`（ファイル一覧指定）。
  System.Drawing で 12×8＝96枚を1枚のJPEGに並べる。
  **全体を8枚おきに間引いて920枚→10シート**にすれば、library全体の傾向が10回で見渡せる。
  「その2」で見つからなかったのは、探し方が**ファイル名検索だけ**だったため。写真の名前は
  `IMG_1234.JPG` なので、名前では絶対に見つからない。
- **iphone_backup のフォルダ名が `YYYYMM_a` なので、季節はフォルダ名で絞れる。**
  春=03〜04、夏=07〜08、秋=10〜11、冬=12〜02。これで四季の写真はすぐ集まる。
- **iPhone写真は EXIF の向き（タグ274）を見ないと横倒しになる。**
  System.Drawing は自動回転しないので、書き出し関数で `RotateFlip` を入れた。
- **`.like-art img` に `position:absolute; inset:0` を効かせているので、
  4枚並べるときは `figure` を `position:relative` にする**だけで各セルに収まる。

### 次にやること
- **バウンドテニスは「男性のデフォルメ画」がまだ無い。** 今は実写を入れている。
  イラストがよければ ChatGPT で作って `image/` に置いてもらえれば差し替える。
  （プロンプト例：「赤いキャップとサングラスの60代男性が、体育館の緑のコートで
  短いラケットを構えているフラットアニメ調のイラスト、白背景」）
- **人が写っている写真の扱い**。今使っている `photo-bt2.jpg` は遠景で顔は判別しにくいが、
  公開前に念のため確認を。アップ写真（`202511_a\IMG_E0567.JPG` など）は
  子どもの顔が写るので今回は使わなかった。
- 公開先（リポジトリ／ドメイン）の決定は「その1」から持ち越し。

### 動作確認
- ローカル（http://localhost:8938 ）で確認
  - 画像24枚すべて表示、読み込み失敗0件
  - 幅1280px … 横スクロールなし。四季の枠 490×320px（各セル 244×159px）、
    バイク＋キャンプの枠 161×168px ×2
  - 幅375px（スマホ） … 横スクロールなし、はみ出し要素0件。四季の枠は 329×320px
  - **この回もブラウザ枠が表示できずスクリーンショットは撮れていない**（数値での確認のみ）。
    見た目の最終確認はロイさん待ち

---

## 2026-07-31（金）その2 — ロイさんの指摘を反映

### やったこと
画像を作りなおすスクリプトは
`C:\Users\roy_s\AppData\Local\Temp\...\scratchpad\build_assets.py`（作業用）。
中身は `dev/tool/nouikiiki-app/クロの画像を作りなおす.py` の `to_black()` を流用している。

- **ヒーローのキャラが間延びして見えた件** … 原因は**切り抜きを詰めすぎたこと**。
  もとの絵は縦2:3だったのに、輪郭ぴったりで切ったので 0.75:1 の横広になり、
  同じ幅で表示すると平べったく見えていた。→ **まわりに40pxの余白を残す**ようにして、
  もとの構図の比率に近づけた（760×990）。表示幅も 400→440px に。
- **瀬戸内の夕日が見えなかった件** … 帯（高さ150px）に対して写真が縦長すぎ、
  `center 58%` で切ると夕日より下だけが出ていた。→ **写真そのものを帯の形に切り出した**
  （元 2200×1041 の y=100〜800。太陽 y≈300 と水平線 y≈616 の両方が入る）。
  帯の高さも 150→260px（スマホ 118→170px）に上げた。
- **プロフィール写真の左下にあった小さなキャラ画像を削除**（`.pf-mini`）。
- **クロを「まっ黒」にして、場面別の絵を追加した**。
  - `kuro.png` … `新しいフォルダー (6)\ChatGPT Image 2025年6月29日 17_51_53.png` を
    黒く直して背景を透過（620×880）。「好きなこと」のクロの枠に使用
  - `kuro-hello / kuro-go / kuro-bye / kuro-welcome / kuro-night` …
    `Pictures\可愛い黒猫の日常\可愛い黒猫の日常１` の8枚から5枚を選び、
    **上の文字を自動で切り落として**黒くしたもの（320px）
  - クロの枠に4コマ並べ、シニアサイトへのボタンの下と、お問い合わせの下に
    「クロのひとこと」を置いた
- ヘッダーのアイコン（`roy-avatar.png`）は、**赤いキャップの位置を自動で探して**顔だけ切るようにした
  （手に持った赤いヘルメットを拾わないよう、上から32%だけ見る）。

### わかったこと・つまずいたこと
- **使える絵は「可愛い黒猫の日常１」フォルダにある。**
  `可愛い黒猫の挨拶`（25枚）は毛がふさふさの別の絵柄。
  サイトで使っているクロと同じ絵柄なのは **`可愛い黒猫の日常１`（8枚）**のほう。
- **切り抜きは「詰めればいい」わけではない。** もとの絵の縦横比が変わると、
  同じ幅で置いたときに絵が伸びた／潰れたように見える。余白ごと残すほうが安全。
- **横長の帯に縦長の写真を敷くのは、CSSの `background-position` では詰められない。**
  写真そのものを帯の形に切り出しておくのが確実（画面幅で切れ方が変わらない）。
- PNGは `quantize(FASTOCTREE)` で色数を落とすと**透過を保ったまま1/4以下**になる。
  ヒーローのキャラは 713KB → 151KB。

### 次にやること（ロイさん待ち）
- **写真がどこにあるか教えてもらう**。以下の4つは、PC内の8,500枚から探しきれなかった：
  1. バイクの写真／キャンプの写真（「バイクとキャンプの人」の枠に2枚並べる）
  2. ツーリングのイラスト（`art-touring.jpg`）の**元になった実写**
  3. バウンドテニスの写真（今は女性のイラストが入っていて、差し替えが必要）
  4. 春・夏・秋・冬の写真（今は桜1枚だけ）
- ~~クロをシニアサイトと教材にも入れる~~ → **2026-07-31 に完了・公開ずみ**。
  くわしくは `dev/site/senior-site/DEVLOG.md` の 2026-07-31 の項。
  クロの画像は両サイトで同じもの（`roys-channel/image/kuro*.png` と
  `senior-site/image/kuro/`）。**片方を作りなおしたら、もう片方にもコピーすること。**

### 動作確認
- ローカル（http://localhost:8938 ）で確認
  - 画像20枚すべて表示、読み込み失敗0件
  - 幅1280px・幅375px とも横スクロールなし（scrollWidth = clientWidth）
  - この回はブラウザ枠が表示できずスクリーンショットが撮れていない。**目視確認はロイさん待ち**

---

## 2026-07-31（金）

### やったこと
- **メインサイト「Roy's Channel」を一から作り直した**（`index.html`）。
  素の HTML＋CSS＋JavaScript の1枚もの。bundler 形式ではないので、**エディタで直接直せる**。
  - ページの並び：ヒーロー → 3つの顔 → **ITシニアなんでも相談室への誘導（いちばん大きい枠）**
    → プロフィール → 好きなこと（バイク／バウンドテニス／黒猫クロ／香川） → 発信（YouTube・note）
    → お問い合わせ → フッター
  - **シニア向けサイトへの導線は3か所**：ヒーローのボタン／3つの顔カードNo.01／
    水色の大きなセクション（赤い大ボタン）＋フッター。リンク先は
    `https://roys1105.github.io/roys-senior-it/`（別タブ）
  - お問い合わせフォームは、シニアサイトと同じ Cloudflare Worker
    （`https://roy-senior-site-api.roy-s1105.workers.dev/contact`）へ送信
  - 年齢はロイさんの誕生日（1964年11月5日）から**自動計算**（今日時点で61歳と表示）
- **PC内から画像を集めて `image/` に書き出した**（縮小・軽量化ずみ）。
  - `roy-chara.png` … `Pictures\バイクイラスト` のちびキャラ＋バイク。**背景を透過に加工**してヒーローの主役に
  - `roy-avatar.png` … 上の顔だけを切り出したもの。ヘッダーのロゴ／ファビコン
  - `roy-chara-front.jpg` `roy-chara-side.jpg` … 立ち姿バージョン
  - `art-touring.jpg` `art-bike-park.jpg` … バイクのアニメ風さし絵
  - `art-bt-coach.jpg` … ラケットを持ったアニメ風さし絵（バウンドテニスの枠に使用）
  - `kuro.jpg` `art-kuro-town.jpg` … 黒猫クロ
  - `photo-setouchi.jpg`（瀬戸内の夕日）`photo-sakura.jpg`（桜）`photo-profile.jpg`（ロイさん本人）
  - `senior-logo.png` `cover-sumaho.jpg` `cover-inaka.jpg` `app-nouikiiki.png` … シニアサイトから流用
- 前からあった `Roy's Channel - ホーム.html`（bundler 形式の下書き）は
  **`_backup/Roy's Channel - ホーム(旧・下書き).html` に退避**した。消してはいない。
- プレビュー用に `dev/tool/.claude/launch.json` へ `roys-channel-preview`（ポート8938）を追加。

### わかったこと・つまずいたこと
- **使えるキャラクターの絵は、すでにPCの中にあった。**
  `C:\Users\roy_s\Pictures\バイクイラスト` に、赤いキャップ＋サングラス＋バイクの
  ロイさんキャラが十数枚。黒猫クロも同じ場所と `新しいフォルダー (6)` にある。
  **新しく描き起こす必要はなかった。**次に絵が要るときも、まずここを見る。
- **背景の透過は「背景色との距離」で消せた。**
  ちびキャラの背景はクリーム色（R245 G238 B230）で、肌色や白ハイライトとは十分離れていたので、
  左上の1ピクセルを基準に「距離26以内→透明、52まで→半透明」で処理してきれいに抜けた。
  白背景の絵は、ヘルメットのハイライトまで抜けてしまうので同じ手は使えない。
- **`img{height:100%}` は、親の高さが auto だと効かない。**
  「好きなこと」のカードで縦長画像が本来の高さ（750px）まで伸びてしまった。
  → 枠を `position:relative` にして、画像を `position:absolute; inset:0` で敷く形に直した。
- **`max-width:520px` だけ指定した画像は、スマホで横にはみ出す。**
  `img{max-width:100%}` を上書きしてしまうため。`width:100%` も一緒に書くこと。
  （シニアサイトのヘッダーロゴのはみ出しも、たぶん同じ原因）
- Worker の CORS は `*` なので、**別ドメインになってもお問い合わせフォームはそのまま使える**。
  実際に localhost:8938 から叩いて、CORS が通ることを確認ずみ。

### 次にやること
- **公開先を決める。** シニアサイトは `roys1105.github.io/roys-senior-it/`。
  メインサイトは別リポジトリ（例：`roys-channel`）にするか、独自ドメインを取るかを決める
- 決まったら、シニアサイト側のヘッダー／フッターにも「Roy's Channel（メインサイト）」への
  **戻りリンク**を足す（今は行きっぱなしになっている）
- Instagram・X をやっているなら「発信」の枠に追加する（今はURLが分からないので入れていない）
- キャンプの写真が見つかっていない。あれば「好きなこと」に1枠足す

### 動作確認
- ローカル（http://localhost:8938 ）で確認
  - 画像15枚すべて表示、読み込み失敗0件、JSエラー0件
  - 幅1280px … 横スクロールなし（scrollWidth = clientWidth = 1265）
  - 幅375px（スマホ） … 最初 542px はみ出していた → シニアロゴを直して 375px に収まった
  - ハンバーガーメニュー … 開閉OK、リンクを押すと閉じる。**画面を広げたときに開いたまま残る不具合を修正**
  - お問い合わせ … 空データを送って `400 missing_fields` が返ることを確認（CORS 通過。メールは飛んでいない）
  - **本番の送信テストはしていない**（実際にロイさん宛のメールが飛ぶため）
